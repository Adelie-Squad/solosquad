# Quirky Task

Something specific to this codebase. Mentions `src/quirky.ts` and `lib/quirky-helper.ts` and `package.json` scripts.

## Steps

1. Build via `npm run quirky`
2. Output goes to `dist/quirky/`
3. CI in `ci/quirky.yml`
