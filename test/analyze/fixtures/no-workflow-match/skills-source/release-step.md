# Release Step

Codebase-specific release procedure.

References `src/server/release.ts`, `lib/version.ts`, `package.json`, `ci/release.yml`, `Dockerfile`.

## Procedure

Run `npm run release` then check `dist/` build output.
