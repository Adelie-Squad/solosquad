---
name: Minimal Skill
description: The smallest possible Anthropic-compatible SKILL.md — only required fields.
---

# Minimal Skill

This SKILL.md demonstrates the absolute minimum: just `name` and
`description` in the frontmatter. Per the Anthropic Agent Skills spec these
are the only required fields. SoloSquad's parser must accept this without
demanding any of its own extensions.
