# Reports INDEX — org 계층 (근거 스냅샷 · cross-repo)

> "무엇이 사실인가" — 제품 관점 조사·근거 스냅샷. PRD 가 `evidence_ref` 로 인용하는 대상.
> 여러 repo 가 필요 시 참조. 신규는 `<name>_<YYMMDD>.md` 규칙. (docs 스킬 §2)

## 신규 (v1.3.8 이후, 본 디렉토리)

| 파일 | hook |
|---|---|
| _(아직 없음)_ | |

## 산재 리포트 — fix-forward (옮기지 않음)

`docs/prd/v1.3.8_docs-management.md` §3.6: **frozen PRD 가 `evidence_ref` 로 참조하는 과거
리포트는 이동하지 않는다**(참조 깨짐 방지 · §3.3 immutable 원칙). 현 위치 그대로 참조:

| 파일 | 현 위치 | 참조하는 frozen 문서 |
|---|---|---|
| `AI_Agent_Harness_Report.md` | `docs/ideation/` | v0.3·v0.4·v0.5·v0.6 PRD 등 evidence_ref |

신규 리포트만 본 디렉토리·명명 규칙을 따른다(과거는 fix-forward).
