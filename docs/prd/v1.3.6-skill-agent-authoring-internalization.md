# v1.3.6 — skill·agent 작성법 내재화 + 자가개선 asset 구조 (도입)

> **상태:** 기획 (2026-06-23). **두 축을 동시에 진행한다.**
> **(A) 작성법 내재화** — "좋은 skill/agent 란 무엇인가"의 표준을 **매니저 skill 에 권위(authoring
> authority)로 박제**하고, 검증기(`validateSkill`/`validateAgent`)를 공식 정량/문체 규칙에 정렬하며,
> 기존 skill/agent 를 이 표준으로 점진 리라이트한다.
> **(B) 자가개선 asset 구조** — asset 이 *스스로 학습·개선*하는 골격을 도입한다(loop engineering 본격
> 도입 *전* 최소 골격). 본 버전은 **①행동층(eval-게이트 텍스트 학습, SkillOpt 식)만** — 채점기(§3.3)
> 갖춘 asset 부터. **②경험층(세션 간 메모리)은 v1.4.0 오케스트레이션 세션으로 이관**(2026-06-23 결정 —
> 메모리는 세션 상태와 묶여 다뤄야 함. §3.5②·§6.3 참고).
>
> **로드맵 위치:** v1.3.5(기획 워크플로우 + 자산 매니저 일관성) → **v1.3.6(skill·agent 세부 고도화)**
> → v1.4.0(세션 오케스트레이션 + 멀티 repo 실행).
>
> **분류:** minor. 신규 런타임 표면보다 **표준·검증·자가개선 골격**이 중심. asset *기능* 추가가 아니라
> *품질·학습 인프라* 강화.

**의존:** v1.3.2(도메인 생애주기 매니저 — `{asset}-manager` 5종·공유 graph/validation/guardrail/naming
코어), v1.3.5(매니저=skill 통일·5종 CRUD 정렬).

**근거(ideation):**
- `docs/ideation/260617-skill-md-authoring-best-practices.md` — Part B.6(공식 skill-creation 3-가이드:
  description 최적화·output eval·작성법), Part F(SkillOpt 자가개선), Part H(적용 전략 H1·H2·H2.5·H3), Part I(체크리스트).
- `docs/ideation/260618-agent-authoring-best-practices.md` — B.5(Vercel Eve filesystem-first),
  **B.6(CMA 1차 하네스 — Outcomes 자가채점·multiagent depth 1·Memory store)**, E.5(자가개선/메모리 패턴),
  Part F(2층 포지셔닝), **Part G 전수(G1 그래프검증·G2 역할중첩·G4 budget·G5 refine + `launch-your-agent`
  4페이즈 생애주기)** → **§3.7 agent-manager 상세설계의 직접 근거**.

---

## 1. 배경 / 문제

SoloSquad 는 5종 자산(skill·agent·workflow·goal·cron)의 생애주기 매니저(v1.3.2)와 CRUD 정렬(v1.3.5)을
갖췄다. 그러나 **"잘 만드는 법" 자체가 시스템 안에 없다:**

1. **작성 표준이 코드/문서에 흩어져 있다.** 좋은 SKILL.md 의 규칙(description 공식·정량 한도·번들
   구조·eval)이 ideation 문서에만 있고, 매니저 skill 은 *얇은 CRUD 오케스트레이터*라 표준을 들고 있지 않다.
2. **검증기가 공식 규칙을 미검사.** `validateSkill` 은 64자·1024자·예약어·kebab·디렉터리일치·3인칭·vague
   를 검사하지 않는다(`anthropics/skills` corpus 라운드트립 호환·마켓 배포 사전조건 미달).
3. **decorative frontmatter 필드의 부채.** `pm_conventions`(~28 skill)·`category` 등은 skill-parser 의
   `known` 집합에 없어 **`extra` 백으로 떨어진다 = 파싱되나 검증·구동 안 됨**. "적혀 있으니 작동한다"는
   착각만 남기고 드리프트 위험을 키운다(§3.4).
4. **"좋아졌다"를 측정할 수 없다.** 개선은 감으로 한다 — description 트리거 eval·output A/B 가 없다.
5. **자가개선 구조 부재.** asset 은 write-once 다. loop engineering 도입 전, *스스로 다듬는* 최소 골격이 없다.

v1.3.6 은 이 다섯을 닫는다. **핵심 통찰: asset 을 "한 번 쓰는 파일"이 아니라 "eval 로 다듬고 스스로
학습하는 자산"으로 본다**(SkillOpt: "skill = frozen agent 의 external state, trained").

## 2. 핵심 개념

- **매니저 = 작성 권위(authoring authority).** `skill-manager`/`agent-manager` 가 단순 CRUD 가 아니라
  **"좋은 작성이 무엇인가"의 단일 기준**을 보유한다. 다른 skill/agent 를 만들거나 고칠 때마다 이 표준을
  적용한다 → addyosmani `using-agent-skills`(meta-skill 라우터) / Anthropic `skill-creator` 의 SoloSquad 판.
- **표준은 점진공개로 담는다.** 매니저 SKILL.md 본문은 lean(요지만), 상세는 `references/` 번들로 — *스킬이
  설파하는 원칙을 스스로 실천*. **공유 작성 표준(~70%)은 `skills/skill-core/core.md` 단일 진실원**, skill·
  agent 고유분(~30%)만 각 매니저 `references/` 에(드리프트 0 — §6.2 결정 A).
- **1차(CMA) 대비 차별화 — "통합"이 아니라 "자산성"(2026-06-23, 260618 §B.6·E.5).** Anthropic 1차 hosted
  하네스 CMA 가 채점(Outcomes)·메모리(Memory/Dreams)·스케줄을 *한 하네스에 융합* — 통합 자체는 더 이상
  우리만의 자리가 아니다. SoloSquad 차별화는 ⑴**선언적 `SKILL.md` 자산**(CMA 는 런타임 API config) ·
  ⑵**정적 그래프 검증**(`validate`, CMA 엔 없음) · ⑶**번들 불변 + org 오버레이**. 반대로 *메커니즘은 적극
  차용* — Outcomes(자동 채점기)·depth-cap(=1)·permission policy(MCP deny-by-default)·Memory store 를 1차 그대로 베낀다.
- **load-bearing vs decorative.** 모든 커스텀 필드는 동작을 구동하거나 검증기가 강제해야 한다. 둘 다
  아니면 추가하지 않는다(§3.4).
- **자가개선 = 2층(개념), 본 버전은 ①만.** ①**행동층**(역할/절차 프롬프트를 eval-게이트로 텍스트 학습 —
  SkillOpt: rollout→reflect→bounded patch-edit `Lt=4`→held-out gate + rejected-edit buffer + protected
  slow-update; **자동 채점기 필수**) + ②**경험층**(세션 간 메모리 누적). 둘은 SkillOpt 의 fast/slow
  2-timescale 과 동형이나, **②는 세션 상태와 결합돼 v1.4.0 오케스트레이션에서 다룬다**(본 버전 비범위).
- **순서 의존:** **채점기(eval) 없이는 행동층 자가개선 없음.** 그래서 §3.3(eval) 이 §3.5①(자가개선)의 전제.

## 3. 스코프

### 3.1 매니저를 작성 권위로 격상 ✅ (skill 측 착수 — 본 PRD 작성 중 1차 구현)
`skill-manager` 를 작성 표준 보유체로 재작성하고, 표준을 `references/` 3종으로 점진공개:
- **`skills/skill-manager/SKILL.md`** — lean 본문 + 3대 요지(description=디스커버리 · 본문=절차적 규율 ·
  decorative 필드 금지) + CRUD 흐름에 표준 적용 단계 삽입(description 먼저·eval 설계). 인접 skill 분담
  명문화(정적=validate, 리뷰=asset-review, 후보식별=skill-refinement, agent=agent-manager).
- **`references/authoring-guide.md`** — description 공식·본문 명명패턴(Gotchas·Verification 게이트·
  anti-rationalization·Red Flags)·정량 한도·**번들 구조(scripts/references/assets) 규칙**·점진공개.
- **`references/frontmatter-fields.md`** — 필드별 load-bearing/decorative **감사** + 작성 체크.
- **`references/eval-recipe.md`** — description 트리거 eval + output A/B + 자가개선 루프 연결.

> **agent 측 동형 — 상세 설계는 §3.7** (2026-06-23 보강). `agent-manager` 를 skill-manager 와 같은 방식으로
> 격상하되, 고유축은 **위임 그래프·역할 중첩·budget 상속·생애주기**(agent-doc G1·G2·G4·G5 + `launch-your-agent`
> 4페이즈). 공유 원칙(description·필드 감사) 재사용 방식은 §6.2 결정. **설계 확정까지만 — 구현 보류.**

### 3.2 검증기 공식 표준 정렬 (P0 — H1)
착수: `src/bot/skill-parser.ts` `validateSkill`, `src/bot/agent-validate.ts`.
- `name`: ≤64 + `^[a-z0-9]+(-[a-z0-9]+)*$`(연속/양끝 하이픈 금지) + 예약어(`anthropic`/`claude`) +
  **부모 디렉터리명 일치**.
- `description`: ≤1024 상한 · 1인칭 린트("I can"/"You can"→warn) · vague 휴리스틱("helps with"→warn) ·
  "Use when"/"사용 시점" 트리거절 부재 시 info.
- 본문 500줄 / 참조 1단계 깊이 린트.
- **agency-agents 식 originality 게이트**(8-word shingle, FAIL ≥40%/WARN ≥20%) **도입 확정**(2026-06-23,
  권장 채택) — skill·agent 양쪽에 역할/설명 중복(re-skin) 탐지.

### 3.3 eval 루프를 corpus 에 (P0 — H2.5, 자가개선의 채점기)
착수: `src/analyze/validator-corpus.ts` + `npm run test:corpus`.
- **description 트리거 eval:** 20쿼리(should 8–10 + near-miss 네거 8–10) × 3런, 임계 0.5, train60/val40.
- **output A/B:** `evals/evals.json` 을 with_skill/without_skill 로, pass-rate + token·duration **delta**.
- 이 두 eval = §3.5 행동층 자가개선의 채점기. **검증기 없는 asset 은 정적 게이트로 폴백.**

### 3.4 frontmatter 필드 감사 — 결정 + 일괄 적용 (P1)
`pm_conventions`·`category` 는 skill-parser 의 `known` 집합에 없어 **`extra` 로 떨어진다 = 파싱되나
검증·구동 안 됨**. 단 `pm_conventions` 4필드는 *의미 없는 게 아니라* 실재 PM 규율이다(출처
`workshop-solosquad-description.md` §175~178) — "진짜 규율인데 읽는 코드만 없는" 케이스.

**값 분포(2026-06-23 실측 28 skill)가 결정을 가른다 — 한 필드가 어디서나 같은 값이면 per-skill 정보가
아니라 글로벌 기본값이다:**

| 필드 | 분포 | 해석 |
|---|---|---|
| `anti_sycophancy` | **28/28 true** | per-skill 정보 0 → **글로벌 규율** |
| `post_labeling` | 26 true / 2 false | 거의 균일 → **글로벌 + 드문 예외** |
| `hard_gate` | 17 false / 11 true | **진짜 가변** → per-skill 신호 |
| `minimum_approaches` | 16×1 / 12×2 | **진짜 가변** → per-skill 신호 |
| `category` | 7개 값 분산 | 그룹 정보 有, 표시용으로만 소비 |

→ **✅ 결정됨(§6.1 = A 하이브리드, 2026-06-23):** `anti_sycophancy`·`post_labeling`(균일) → **글로벌화**
(28 skill 에서 제거 + Chief/PM base 1곳 wire) · `hard_gate`·`minimum_approaches`(가변) → **wire(load-bearing
승격)** · `category` → list 그룹화에 쓰면 enum 강제, 아니면 드롭. **전 skill 통일 적용**(드리프트 방지).
**새 decorative 필드 추가는 지금부터 금지.** (구현 보류 — 결정만 확정.)

### 3.5 자가개선 — ①행동층만 (P1 — loop engineering 전 최소 도입)
- **①행동층(SkillOpt 식):** `skill-author`/refine 위에 rollout→reflect(성공/실패 분리)→bounded patch-edit
  (`Lt=4`/floor 2)→held-out gate + rejected-edit buffer + protected slow-update. **§3.3 채점기 있는
  skill 부터.** 목표 본문 ~920토큰. optimizer 별도 모델 불필요(matched 도 동작).
- **②경험층(메모리) — ⛔ 본 버전 비범위, v1.4.0 오케스트레이션으로 이관(2026-06-23 결정).** 근거: 세션 간
  메모리는 **세션 상태·오케스트레이션과 결합**해 다뤄야 함(per-agent 스코프는 agent-doc G6 = v1.4+). 설계
  자료(고정 taxonomy·human-approved write·full-block replace 등)는 §6.3 + agent-doc E.5 에 보존 → v1.4.0 착수.

### 3.6 통합 입구 축소 + CLI 통칭 명사 제거 (P2 — ✅ 결정 2026-06-23, §6.4)
**결정: CLI 에서 통칭 명사(`asset`/`primitive`)를 *완전히* 뺀다.** 이유 — 통합 입구 `solosquad asset *` 는
개별 그룹(`skill`/`agent`/…)과 **이중**이었고(예 `asset list skill`==`skill list`), 통합이라야 의미 있는 건
**cross-kind 일괄 작업**뿐. "kind 선택→개별 흐름"은 CLI 가 아니라 **chat(Chief 라우팅)**의 몫(인터랙티브
셀렉터는 스크립트성 파괴 + chat 중복).
- **폐기:** `solosquad asset list|show|validate` **일괄 제거**(개별 그룹과 중복분). publish 됨 → deprecation
  warning 후 제거(§6.4-B 창: v1.x warn → v2.0 제거).
- **승격:** 유일한 cross-kind 값인 *전체 검증*은 **명사 없이 top-level `solosquad validate`**(종류 생략=skill·
  agent·workflow·cron 전부, CI 게이트). 디스커버리는 기존 `solosquad commands` 가 담당.
- **개별 그룹 불변:** `skill`/`agent`/`workflow`/`goal`/`cron` 그룹은 그대로(각자 "kind 선택 후 verb" 트리).
- **통칭어(산문/내부):** 문서·코드 주석의 통칭은 `primitive` 로 통일(번들 `assets/` 폴더와의 단어 충돌 회피).
  **CLI 엔 통칭 명사 부재**(노출 안 함). **`assets/` 번들 폴더는 불변.**

### 3.7 agent-manager 작성 권위 격상 — 상세 설계 (P1 — §3.1 agent 측 / 설계 확정, **구현 보류**)

**근거.** agent-doc Part G(G1~G5) + **`anthropics/launch-your-agent` 생애주기 스킬**(interview→stage/launch
→grade/iterate→schedule = 우리 asset-manager 의 1차 거울상, agent-doc G 보강). skill-manager(§3.1·부록 A)와
**동형 격상**하되, agent 고유축 = **위임 그래프·역할 중첩·budget/HITL·생애주기**. 현재 SKILL.md 는 42줄 얇은
CRUD — 작성 표준을 *보유하지 않음*.

**(a) 공유/고유 분담** (방식은 §6.2 결정 — 권장 A: 공유 코어 + 도메인 델타).
- **공유 코어(~70%)**: description 공식 · name 한도(≤64·kebab·dir일치·예약어) · 본문 절차적 규율+명명패턴 ·
  번들 구조 · 점진공개 · 필드 감사(load-bearing vs decorative) · eval 골격(trigger+output A/B). →
  **`skills/skill-core/core.md` 단일 진실원**(§6.2 결정 = A, 폴더명 `skill-core`로 확정·2026-06-23 작성됨).
  skill-manager·agent-manager 양쪽이 참조.
- **agent 고유(~30%) = `agent-manager/references/` 4종**:
  - **`delegation-graph.md` (G1)** — collaborators/used_by/skills_used 참조 무결성 + Kahn 순환 + **depth cap
    (CMA coordinator depth 1·Claude 5·OpenAI 10·LangGraph 25 선례)** + tier↔team 정합 + budget narrower-only
    사전표면화. (= `agent validate --graph` 의 작성 가이드면)
  - **`role-boundary.md` (G2+C)** — SRP·right-altitude·generalist 함정 + **8-word shingle 중복 게이트
    (FAIL≥40%/WARN≥20%)** + **"single agent first; multiagent only when earned; 값진 subagent 먼저,
    coordinator 나중"**(launch-your-agent 1차) + description=위임 트리거 충분성(Eve 컴파일러 강제계약 차용).
  - **`lifecycle.md` (launch-your-agent 이식 — 핵심 신규)** — 4페이즈를 C/R/U/D 에 사상(아래 b) + **v0/v1/v2
    scoping**(reason class: not possible / needs credential / scope, per-agent NEXT-DIRECTIONS 적재) +
    **rubric = 수용 게이트**(3~6 binary·job-specific·"리뷰 없이 spawn 할 자신이 드는가", CMA Outcomes 패턴) +
    골든케이스 regression.
  - **`guardrails.md` (G4+E.4)** — turn/depth budget **자식 상속**(LangGraph SubAgentMiddleware 반면교사) +
    circuit breaker + 비가역 액션 HITL(permission policy, **MCP deny-by-default**) + downstream 전 출력검증.

**(b) SKILL.md 본문 재작성 — lean + launch-your-agent 4페이즈를 C/R/U/D 에 사상.**
- **C(생성) = interview → 자산인지 → scaffold(v0) → 정체성 → 그래프검증 → 수용게이트:**
  1. **interview(클러스터형 enumerable, AskUserQuestion)** — job/done/inputs/outputs/cadence/boundaries/
     learning/shape 8클러스터. 경계는 *맥락에서만* 짧게. 서술이 2~4클러스터 동시 답하면 남은 것만 묻는다.
  2. **자산 인지(기존 유지·강화)** — `agent list` 로 중복 확인. 겹치면 위임 안내(**single agent first**),
     정말 새 역할일 때만 생성. coordinator-first 금지.
  3. **scaffold(v0)** — `agent new`(무-LLM). **핵심 역할만**, 나머지는 per-agent NEXT-DIRECTIONS(reason class).
  4. **정체성 작성** — description=위임 트리거 공식 + triggers/collaborators/skills_used/used_by + 본문
     (역할·입력·출력·위임).
  5. **그래프 검증** — `agent validate --graph`.
  6. **수용 게이트(rubric)** — 3~6 binary 로 "이 actor 를 리뷰 없이 spawn 할 자신이 드는가" 자가채점
     (CMA Outcomes 패턴). 미달 시 description/본문 sharpen 후 재검. (skill 측 eval-recipe 의 agent 판)
- **R(조회)** — 기존 `agent list`/`show`(위임 엣지 포함).
- **U(수정)** — `agent-profile.yaml` modifier / 본문 편집 → 재검증. **자가개선 연결 명문화:** 행동층 refine
  (§3.5①, 본문·tone, frontmatter 그래프는 protected) · 경험층 메모리(§3.5②).
- **D(삭제)** — 번들 불변, org actor 만. 파괴적 동작 적용 전 확인.
- **schedule(호출 ③층)** — 반복 actor 는 `cron-manager` 로 연결(launch-your-agent Phase 4).
- **인접 분담 명문화** — 정적=`agent validate --graph` · 리뷰=`asset-review` · 후보식별=refinement(공유) ·
  수용채점=lifecycle.md rubric · skill=`skill-manager`.

**(c) 검증기(`validateAgent`) 정렬은 §3.2 와 합류** — name/desc 공식 + originality 게이트는 공유, 그래프
무결성·depth cap 은 agent 고유.

> **구현 보류 경계:** 본 §3.7 은 *설계 확정*까지다. **공유 코어 `skill-core/core.md` 는 작성 완료**(§6.2).
> 잔여 후속 구현 = ⓐ agent-manager SKILL.md 본문 재작성 · ⓑ `agent-manager/references/` 4종 신규 ·
> ⓒ `validateAgent` 정렬(§3.2) · ⓓ **공유 코어 배선**(skill-manager 기존 `references/` 3종의 공통분 제거 →
> skill-core 참조 전환 리팩터). 본 버전에선 PRD 반영 + 코어 md 까지.

## 4. 비범위

- loop engineering 본체(24/7 자율 개선 루프 가동) — 본 버전은 *골격*만(①행동층·채점기). 가동은 후속.
- **②경험층(세션 간 메모리) — v1.4.0 오케스트레이션 세션으로 이관**(2026-06-23). 세션 상태와 결합 사안.
- 행동층 자가개선의 *주관적 output* skill 적용(자동 채점기 없음) — 정적 게이트(validate+asset-review)로 폴백.
- 마켓플레이스 배포 경로(`plugin.json`/kebab 강제, H4) — 외부 확산 시점으로 보류.
- workflow/goal/cron 매니저의 작성 권위 격상 — skill·agent 우선, 나머지는 패턴 검증 후.

## 5. 산출물 / 완료 기준

- [x] `skill-manager` 작성 권위 격상 + `references/` 3종(authoring-guide·frontmatter-fields·eval-recipe). 947 test green.
- [x] `agent-manager` 동형 격상 **설계 확정**(§3.7 — 4페이즈 생애주기·references 4종·수용게이트 rubric). 구현 보류.
- [x] 공유 코어 `skills/skill-core/core.md` 작성(§6.2 결정 A — description·정량한도·본문규율·번들·점진공개·필드감사·eval 골격).
- [x] 공유 코어 **배선**: skill-manager `references/` 공통분 제거(authoring-guide 삭제, frontmatter-fields·eval-recipe 트림→core 참조) + SKILL.md repoint. **agent validate green**.
- [x] `agent-manager` 구현(§3.7-ⓐⓑ): SKILL.md lean 재작성(4페이즈→C/R/U/D, core.md 참조) + `references/` 4종 신규(delegation-graph·role-boundary·lifecycle·guardrails). **agent validate green**.
- [x] description **YAML-safe** 수정(skill·agent-manager `사용 시점:`→`사용 시점 —`, core.md §2 공식·경고 반영) — 기존 skill-manager 파싱 깨짐 동시 해소.
- [ ] `validateSkill`/`validateAgent` 공식 표준 정렬(§3.2) + 테스트.
- [ ] corpus eval 루프(§3.3) — trigger-rate + output A/B 골격.
- [x] 필드 감사 **결정 확정**(§3.4·§6.1 = A 하이브리드). 구현(글로벌화+wire+enum, 전 skill 일괄) — 후속.
- [ ] 자가개선 **①행동층** 골격(§3.5①, 채점기 갖춘 skill 부터). **②경험층은 v1.4.0 이관.**
- [ ] 통합 입구 축소 + CLI 통칭 명사 제거(§3.6·§6.4): `asset *` 폐기(warn→v2.0) + `solosquad validate` 승격. 산문 통칭=`primitive`.
- [ ] 4-docs 게이트(roadmap·architecture §13.6·master-guide ko/en) + CHANGELOG.

## 6. 결정 기록 (2026-06-23 — 전 항목 해소)

> §6.1 ✅(A) · §6.2 ✅(A·skill-core) · §6.3 ⏭️(v1.4.0 이관) · §6.4 ✅(CLI 명사 제거). **열린 결정 없음.**
> 모든 결정 = *확정*, 구현은 보류(설계·코어 md 까지). 아래는 결정 내용 + 근거 기록.

### 6.1 §3.4 — pm_conventions·category 처리 ✅ 결정됨 (A — 하이브리드)
> **(2026-06-23 결정)** **A 채택** — 균일 필드(`anti_sycophancy`·`post_labeling`) 글로벌화 + 가변 필드
> (`hard_gate`·`minimum_approaches`) wire 승격 + `category` enum강제/드롭. 전 skill 일괄. **구현 보류.**
분포(§3.4)가 "전부 wire / 전부 제거"의 이분법이 아니라 **필드별 갈림**임을 보여준다.
- **(A) 데이터 기반 하이브리드 — ✅ 채택.**
  - `anti_sycophancy`(28/28)·`post_labeling`(26/2) → **글로벌화**: per-skill frontmatter 에서 제거하고
    **PM 모드 세션 기본값으로 1곳 wire**(Chief/PM base 지침 주입). 예외(post_labeling:false 2개)는 해당
    skill 본문에 사유와 함께. 효과: 28곳 중복·드리프트 제거 + 실제 작동.
  - `hard_gate`(17/11)·`minimum_approaches`(16/12) → **wire(load-bearing 승격)**: 진짜 가변이므로
    제거하면 신호 손실. skill 실행 시 런타임이 읽어 강제(`minimum_approaches:2`→"≥2 접근 비교" 주입,
    `hard_gate:true`→exit_criteria 게이팅). 주의: `hard_gate` 는 *workflow stage* 레벨에선 이미
    `workflow-validate.ts` 가 읽음 — skill 레벨 의미와 정합 필요(충돌 아닌 레이어 구분).
  - `category`(7값) → `solosquad skill list` 그룹화에 쓰면 enum 검증 후 load-bearing, 아니면 폴더서
    유도 가능하니 **드롭**. 남기면 **반드시 enum 강제**.
- **(B) 전부 런타임 wire** — 4 convention 모두 연결 + category enum. 비용: 균일 필드(anti_sycophancy)도
  per-skill 표면으로 영구화(정보량 0인데 유지).
- **(C) 전부 제거** — frontmatter 에서 삭제, 규율은 본문 산문으로. 가장 싸나 hard_gate·minimum_approaches
  의 per-skill 신호를 잃고 본문 수동 기재에 의존.
- 착수점: wire 시 스킬 프롬프트 조립부(`spawn-assembler` 등) + skill-parser `known` 집합 추가. 글로벌화
  시 Chief/PM base 지침 1곳 + 28 skill 에서 2필드 삭제(마이그레이션).

### 6.2 §3.7 — agent-manager 작성지식 공유 방식 ✅ 결정됨 (A — 공유 코어)
> **(2026-06-23 결정)** **A 채택** — 공유 코어 `skills/skill-core/core.md` 단일 진실원, skill·agent 매니저
> 양쪽 참조(폴더명 `skill-core` 사용자 확정). **코어 md 작성 완료.** 남은 구현(보류): skill-manager 의
> 기존 `references/` 3종에서 공통분 제거 후 skill-core 참조로 전환(리팩터) + agent-manager `references/` 4종 신규.
> 아래 (A)/(B) 비교는 결정 근거 기록(historical).
- **(A) 공유 코어 + 도메인 델타 — 분석상 권장.** 공통 70%를 단일 진실원(예 `skills/_authoring/core.md`)에,
  고유 30%만 각 매니저 `references/`(skill: triggers·번들·loop_mode / agent: 위임그래프·역할중첩·budget).
  장점: **드리프트 0**(우리가 설파하는 single-source 원칙과 일관). 단점: SKILL.md 번들 "references 폴더내
  1단계" 규약과 어긋나 마켓 export 시 inline 필요(=H4, 후순위).
- **(B) 각자 완전 보유(복붙)** — 폴더 자기완결·portable, 단 **DRY 위반 → 공통 규칙 2곳 동기화 부담**(우리가
  경고한 안티패턴).
- ~~**(C) agent-manager 보류**~~ — 폐기(사용자 지시 2026-06-23: 격상 설계 확정). 단 **구현 시점**은 별개로
  보류(§3.7 경계) — 본 버전은 설계만.

### 6.3 §3.5 ②경험층(메모리) — ⏭️ v1.4.0 이관 (본 버전 결정 불요)
> **(2026-06-23 결정)** 메모리는 세션 상태·오케스트레이션과 결합 → **v1.4.0 오케스트레이션 세션에서 결정·구현.**
> 아래 옵션은 v1.4.0 착수 시 결정 자료로 보존(historical).
**(1) 저장 범위:**
- **(A) org 메모리 한정 — 권장.** org 1개 공유 store + 고정 taxonomy(MEMORY.md `type` enum 재사용).
  per-agent 메모리 스코프는 agent-doc G6 비범위(v1.4+ — CMA Memory store 8/session 모델 참조). v1.3.6 은 1층만.
- **(B) org + agent-profile 까지.** 에이전트별 메모리 스코프. 비용: 격리·드리프트·검증 증가 → 골격엔 과함.

**(2) human-approval UX 경로:**
- **(i) Chief 채팅 표면화 + 명시 확인 — 권장.** Chief 가 "이걸 기억할까요?"로 제안 → 사용자 y/n →
  full-block replace. personal-agent-template `needsApproval:always()` 의 SoloSquad 판(메신저 경로와 일치).
- **(ii) CLI confirm.** `solosquad memory add` → diff → 확인. 직접 조작용 보조.
- 공통 규율(skill-core 반영): 턴당 1회·휘발성 거부·환각 방지·full-block replace(델타 X).

### 6.4 통합 입구·CLI 통칭 명사 ✅ 결정됨 (축소 + 명사 제거)
> **(2026-06-23 결정)** 통합 입구를 **축소**하고 **CLI 에서 통칭 명사(`asset`/`primitive`)를 완전히 뺀다**(§3.6).
> - `solosquad asset list|show|validate` **일괄 폐기** — deprecation warning 후 제거(v1.x warn → **v2.0 제거**).
> - cross-kind 전체 검증만 **`solosquad validate`(명사 없는 top-level)**로 승격. 디스커버리=`solosquad commands`.
> - 개별 그룹(`skill`/`agent`/`workflow`/`goal`/`cron`) 불변. 산문/내부 통칭어만 `primitive`. `assets/` 폴더 불변.
> - **인터랙티브 "kind 선택" 단계는 CLI 에 도입하지 않음** — kind 라우팅은 chat(Chief)의 몫(스크립트성·중복 회피).
>
> 검토 기록(historical): 통합 입구의 유일한 비중복 가치 = cross-kind 일괄(validate). show/per-kind list 는 개별과 중복.

---

## 부록 A — 본 버전에서 확정·구현된 것 (2026-06-23)

`skill-manager` 가 더 이상 얇은 CRUD 오케스트레이터가 아니라 **작성 표준의 권위**다:
- SKILL.md 본문 75줄(lean) + 작성 표준 3대 요지 + 표준 적용 CRUD 흐름.
- `references/authoring-guide.md`(73줄) · `frontmatter-fields.md`(43줄) · `eval-recipe.md`(55줄) — 모두
  점진공개(필요 시만 로드), 100줄 미만(참조 규칙 자체 준수).
- 핵심 박제: **description 공식 · 정량 한도 · scripts/references/assets 규칙 · eval 검증(trigger-rate +
  output A/B) · 필드 감사(load-bearing vs decorative, "decorative 필드 금지")**.
- 인접 skill 분담 명문화: 정적=`skill validate`, 리뷰=`asset-review`, 후보식별=`skill-refinement`,
  자가개선 채점=eval-recipe, agent=`agent-manager`.
