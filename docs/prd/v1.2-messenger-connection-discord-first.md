# v1.2 — Messenger Connection (Discord 우선 · Slack v1.2.x)

> v1.1 의 *Team-Centric 5-layer 아키텍처* 의 **L1 Gateway** 를 구체화. Discord 우선 + Slack v1.2.x sub-version 분리. v1.0.4 의 G (config.yaml 자동 생성) + H (Slack author-guard cleanup) + P (5-hop binding diagnostic) 를 본 plan 으로 *완전 흡수* (v1.0.4 별도 publish 없음). 5-hop → **9-hop diagnostic** 일반화 (multi-bot 환경). v1.1 의 *5 main bot (chief + pm + engineer + designer + marketer)* 을 메신저 표면에서 실현 — Bot Identity Registry + Forum Channel + Mention-only Intent + Echo Loop 3중 가드 + Thread Token Budget. 14 reference + OpenClaw 18-platform 사례 + Hermes V2 §3.3 (Webhook persona 약화) 합의.

**로드맵 위치:** v1.1.0 ✓ 출시 (2026-05-27, multi-agent team architecture) → **v1.2 본 plan (메신저 연결 — Discord)** → v1.2.1+ (Slack sub-version) → v1.x (대시보드 · 지식 온톨로지 · LLM backend)

**의존:**
- **v1.1.0** (에이전트 팀 구조 — Chief + 4 main bot + 20 specialist 분리, SKILL frontmatter v2: `tier`/`team`/`category`/`used_by`/`collaborators`/`skills_used`/`pm_conventions`, agents/main/ 평탄 폴더, `composition.yaml` 팀 멤버십, `<org>/agents/main/chief/SKILL.md` 도메인 customized) — *hard dependency, **이미 출시됨***
- v0.8.0 (`<org>/.solosquad/users/<handle>.yaml` + `resolveBotIdentity` + N:1 봇)
- v1.0.1 (`@<slug>` mention parser + multi-repo intent)
- v1.0.3 (Discord guild-org binding + ensureChannels + category lookup)
- v1.0.4 (Discord config.yaml load-or-empty + 5-hop diagnostic 일반화 base)

**기준 버전:** v1.2.0 (Discord) → v1.2.1+ (Slack). 신규 인프라 5건 (Bot Identity Registry + Forum Channel topology + 9-hop diagnostic + Thread Token Budget + handoff-trace.jsonl) + v1.0.4 hotfix 흡수 (G + H + P + L + M + N + O 잔여). 새 CLI 명령 2건. schema 변경 2건.

**분류:** Minor (semver). v1.1 와 분리된 narrative — *외부 가시 UX 변화* 중심. v1.0.x 시리즈의 patch-acumen 위에 *multi-bot 환경의 일반화*.

**범위 분리:**
- **v1.2 (본 plan):** L1 (Gateway). 메신저 연결 · channel topology · Bot Identity Registry · 9-hop diagnostic · Forum Channel · Mention routing · 안전망. v1.0.4 G+H+P+L+M+N+O 흡수
- **v1.1 (별도 plan, 출시 완료):** L2~L5. Chief + 4 main bot + 20 specialist + 18 skill + 4 team. PRD §1-§20, `docs/prd/v1.1-multi-agent-team-architecture.md`.

---

## 0. v1.1.0 출시 후 본 plan 의 위치

### 0.1 v1.1.0 출시 박제 (2026-05-27)

**main HEAD 기준 사실:**
- v1.1.0 정식 출시. `package.json.version = "1.1.0"`. v1.0.2 → 1.0.3 → 1.0.4 → 1.1.0 migration chain 완성.
- `feat/v1.1-multi-agent-collaboration` 가 `main` 으로 병합 (commit `58a25ed`). v1.1 PRD 가 `docs/prd/v1.1-multi-agent-team-architecture.md` 에서 본 doc 보다 상위 dependency.
- v1.0.3 / v1.0.4 patch 가 v1.1.0 에 흡수되어 단일 chain (1.0.4.x → 1.1.0 migration `from` 박제).
- 본 v1.2 plan 은 *작성 시점 (2026-05-24~)* 에 v1.1 plan 만 있던 가정이었으나, **현재는 v1.1.0 코드/번들이 실제 존재**. 본 plan 의 모든 *v1.1 가정* 은 *v1.1.0 출시 사실* 로 갱신.

### 0.2 v1.1.0 출시 후 본 plan 영향 (재정렬)

| 본 plan 의 가정 (작성 시) | v1.1.0 출시 사실 | 영향 |
|---|---|---|
| SKILL frontmatter 의 `bot_name` 필드 (v1.1 슬롯 추정) | v1.1.0 은 `tier` (leader/member) + `team` + `used_by: [bot_name]` 패턴 채택. `bot_name` 단일 필드 미존재 | §4.1 Bot Identity Registry 의 SKILL ↔ 봇 매핑은 `team` + `used_by` 두 필드 조합으로 lookup |
| "5 코어 봇 직원화" 추상화 | v1.1.0 은 *5 main bot* — chief (org level, 사용자 customized) + pm (workspace bundle, 자율 분석가) + engineer + designer + marketer | §3.2 봇 정의 표 v1.2 작성 시 `chief` 가 *사용자 메시지 진입점* (v1.1 §5 Chief Sub-System) |
| "PM session" = 사용자 대면 단일 봇 가정 | v1.1.0 은 **Chief** 가 사용자 대면 (구 PM session 의 격상). PM 은 사용자와 직접 대화 안 함 (Chief 경유) | 메신저 채널에서 *주 응답자* = chief. PM 의 응답은 chief 가 wrap |
| `assets/orchestrator/` (v1.0.x PM SKILL) | `<org>/agents/main/chief/SKILL.md` (v1.1 org-customized) | bot startup 시 system prompt 출처 변경 |
| `assets/agents/{team}/{name}/` (v1.0.x 25 specialist) | `agents/specialists/{name}/` 평탄 + `teams/{team}/composition.yaml` 멤버십 (v1.1 20 specialist) | mention routing 의 specialist lookup 은 composition.yaml 경유 |
| "v1.1 §5.1 PM Supervisor" | v1.1 §6 PM Sub-System (Chief 가 dispatch) | mention routing 코드 주석 갱신 필요 |
| "v1.1 §10.1 bot-health-check routine" | v1.1.0 `schedules/bot-health-check.md` ✓ 출시 | 본 plan §12 의 cron은 schedules/ 사용 |

---

## 0.x v1.0.4 흡수 박제 (역사적 reference)

### 0.x.1 v1.0.4 처리 (사용자 결정 — 흡수 옵션 (a) 유지)

**현재 상태 (commit `58a25ed` 기준 — 본 doc 작성 후 v1.1.0 출시까지 누적):**
- branch `feat/v1.1-multi-agent-collaboration` (v1.0.4 branch 에서 rename, v1.1.0 출시 후 `main` 에 병합 완료)
- v1.0.4 commit 보존:
  - `c990cf3` release(v1.0.4): discord config.yaml auto-create + slack author-guard cleanup
  - `32c0dde` feat(v1.0.4): adopt Best Practice P (partial) — 5-hop binding diagnostic + 9-reference research pinned
  - `a910e22` chore(v1.0.4): reorganize docs/plan → docs/prd + add multi-agent-directory ideation
- local git tag `v1.0.4` (미푸시, npm publish 0회)
- v1.0.4 → v1.1.0 chain (`src/migrations/scripts/1.0.3-to-1.0.4.ts` + `1.0.2-to-1.1.0.ts` with `from: "1.0.4.x"`) ✓ 등록

**흡수 절차 (완료):**
1. v1.0.4 commit 보존 — narrative 박제 그대로 (자가비판 + 9-reference 조사 결과 그대로) ✓
2. v1.0.4 npm publish skip — 1.0.3 → 1.0.4 → 1.1.0 단일 chain ✓
3. package.json 의 1.1.0 bump ✓ (v1.1 commit `0eb3916`)
4. local tag `v1.0.4` 삭제 보류 (사용자 결정. 미푸시 상태이므로 영향 0)

### 0.x.2 v1.0.4 §7.2 의 L+M+N+O 잔여 — v1.2 흡수

`docs/prd/v1.0.4-messenger-config-auto-create.md` §7.2 가 박제한 *9-reference 조사 + 5 Best Practice* 중 L+M+N+O 잔여는 본 plan 으로 흡수:

| BP | v1.0.4 § 7.2 상태 | v1.2 처리 |
|---|---|---|
| **L** 페어링 코드 + approve CLI | v1.1 슬롯 표시됨 | **v1.2.x 후속** — multi-bot 환경에서 봇마다 approve 흐름 별도. v1.2.0 skip |
| **M** snowflake branded types | v1.1 또는 v1.2 슬롯 | **v1.2 부분 흡수** — `bot_user_id` 가 `BotUserId` branded type 도입 (Bot Identity Registry 의 type-level guard). 전체 audit 은 v1.x |
| **N** silent early-return 전수 제거 | v1.1 슬롯 | **v1.2 흡수** — multi-bot 환경의 모든 silent-bail (bot startup · mention 라우팅 · forum post 생성) 이 9-hop diagnostic 으로 attributable 화 |
| **O** token precedence | v1.0.5 patch | **v1.2 흡수** — `bot_token_env` 별 환경변수 + `.solosquad/.env` + CLI flag 명시 precedence |
| **P** actionable hint | v1.0.4 partial 적용 (commit `32c0dde`) | **v1.2 전체 적용 + 9-hop 일반화** (§8) |

---

## 1. 핵심 질문 & 성공 기준

| 핵심 질문 | 성공 기준 |
|---|---|
| 5 main bot (chief/pm/engineer/designer/marketer) 직원화 시 Discord verified-app 한도 부담은? | §3.2 — 본 사용자 1-org 시나리오 = 75 server 미달. *공식 배포* 시 단계적 인증 |
| `#works-<handle>` 의 Discord thread 24h auto-archive 문제는? | §3.3 — **Forum Channel** 격상. 각 과제 = forum post (영구 + 태그) |
| Webhook persona overlay 채택 시점은? | §3.5 — Hermes V2 가 강조 약화 → **v1.2.0 skip, v1.2.x sub-version 슬롯**. 별도 봇 × 5 우선 |
| 봇 ↔ 봇 멘션의 echo loop 방지 mechanism? | §6.1 — 3중 가드 (self-author + bot rate-limit + content-hash dedup) |
| Anthropic 측정 15× 토큰 폭증을 어떻게 막나? | §6.2 + §6.3 — Hop counter (per-thread max 6) + Thread token budget (200k / 50 msgs) + Circuit breaker |
| v1.0.4 의 5-hop diagnostic 가 multi-bot 에서 어떻게 일반화되나? | §8 — **9-hop diagnostic** (기존 5-hop + 4-hop multi-bot 추가) |
| Slack 분리는 어느 v1.2.x sub-version 슬롯? | §10 — v1.2.1 = Forum equivalent, v1.2.2 = `app_mention` + `message.im`, v1.2.3 = tag-based routing, v1.2.4 = canvas + thread |

---

## 2. 외부 reference 합성

### 2.1 14 reference (v1.0.4 §7.2 9 + v1.1 plan 추가 5)

본 plan §11 부록 A 의 14 reference 표 참조. 핵심만:

| # | Reference | 차용 패턴 (v1.2) |
|---|---|---|
| 1 | OpenClaw | botUserId routing — Best Practice Q (§4 Bot Identity Registry) |
| 2 | Claude Code Channels | "Don't auto-pick even when only one" — v1.2.x 후속 (L 페어링 + approve) |
| 6 | llmcord | Env-only config — Best Practice O (token precedence §0.2) |
| 7 | openai/gpt-discord-bot | Env-only 동일 |
| 9 | AnythingLLM | Bot rate-limit + dedup — Best Practice S (§5 + §6.1) |
| 10 | Agno (Phidata) | `SlackTools` + `SlackAPI` first-class — *18-platform 통합 패턴* |
| 11 | MS Agent Framework | Handoff synthetic tool injection — Best Practice U (§9 handoff trace) |
| 13 | OpenAI Agents SDK | Tracing first-class — Best Practice Z (§9 handoff-trace.jsonl 구체) |
| 14 | Salesforce Slackbot MCP | 사용자는 1 봇만 본다 (PM 단일 voice) — 개선 #6 (§5.5) |

### 2.2 OpenClaw 18-platform 사례

`docs/ideation/2026-05-23-multi-agent-v1.1-synthesis.html` §3.3 박제:
- OpenClaw = TypeScript + Node.js + local-first gateway
- 18 platform: Discord/Slack/Telegram/WhatsApp/Signal/Feishu/WeCom/QQBot/...
- **본 plan 의 영감 — 단 v1.2.0 = Discord only**. WhatsApp/Telegram 확장은 *v1.x post-launch*

### 2.3 Hermes V2 §3.3 (Webhook persona 약화) 박제

`docs/ideation/multi-agent-directory.md` V2 (2026-05-23):

> L1 Gateway = "사용자 접점 및 에이전트 가상 페르소나 출력"

V2 가 V1 의 "단일 봇 + Webhook persona" 명시 박제 → "가상 페르소나 출력" 한 줄로 약화. 따라서:
- v1.2.0 = **별도 봇 × 5 (직원화) 만**
- Webhook persona overlay (옵션 d) = **v1.2.x sub-version** 슬롯 — 긴급도 낮음

### 2.4 14 reference 외 추가 (2026-05-24)

- **Mastra (TypeScript)** — Vercel AI SDK first-class + Supervisor pattern. *바이브코딩 사용자 segment* 정합. v1.x deployment model 재설계 시 reference
- **nanobot (HKUDS)** — OpenClaw 의 4k lines 경량 Python fork. MCP first-class

---

## 3. Channel Topology — `#command` + `#works` (Forum)

### 3.1 `#command-<handle>` — 사용자 ↔ PM only

**구조:**
- 채널 가시성: org guild 의 카테고리 `solosquad` 아래 (v1.0.3 §13.6.16 정합)
- 멤버: 사용자 1명 + Chief 봇 1명. *다른 main / specialist 봇은 명시적으로 제외*. v1.1 의 Chief 가 유일한 사용자 대면 봇 (PM 도 여기서 사용자와 직접 대화 안 함)
- 메시지 라우팅:
  - 사용자 → Chief 봇 (전체 listen, 4-channel router 적용)
  - Chief 봇 → 사용자 (요약 + cite + 필요시 forum link)
- DM passthrough: Chief 봇은 Discord DM 도 listen → 응답은 *`#command-<handle>` 채널 cross-post* (audit log)

**Application-level author-guard:**
- v1.0.2 single-bot author-guard 정신 → multi-user 일반화
- 메시지 sender 가 channel.allowed_user_ids allowlist 에 있는지 Chief 봇이 application-level check
- 채널 ACL (Discord role / Slack workspace) 만 의존 안 함 — 다른 사용자 초대 시 Chief 봇이 무시 + audit log

### 3.2 `#works-<handle>` — Discord Forum Channel

**구조:**
- 채널 타입 = **Forum Channel** (Discord 표준)
- 카테고리: `solosquad` (v1.0.3 동일)
- 멤버: 사용자 1명 + Chief + 다른 4 main bot (pm / engineer / designer / marketer). Chief 는 dispatch + synthesize, 4 main 은 자기 팀 specialist 결과를 wrap 해서 forum post 작성
- 각 *과제 / workflow / goal cycle = forum post 1개* (영구 + 24h auto-archive 면역 + 검색 + 태그)

**Forum tags (PM routing 의 1차 hint):**
- `bug` `feat` `refactor` `docs` `design` `review` `urgent` `experiment` `solved`
- Chief 봇이 post 생성 시 task 의 1차 tag 자동 부여 (사용자 의도 → triage skill 의 4-way 분류 + SKILL frontmatter `category` → forum tag 매핑 — v1.1 §5.2 TRIAGE / §9.1 category 정합)

**v1.1 의 workflow / goal cycle 정합:**
- workflow 1개 = forum post 1개 — `<org>/workflows/<wf-id>/_status.yaml` 의 `forum_post_url` 필드 신설
- goal cycle 1개 = forum post 1개 — `<org>/goals/<id>/_events.jsonl` 의 `forum_post_url` 박제

### 3.3 Forum Channel 의 *영구성* 이유 (Best Practice U)

`docs/ideation/2026-05-23-multi-agent-v1.1-synthesis.html` §10.U 박제:

| 측면 | Discord standard Thread | Discord Forum Channel |
|---|---|---|
| Auto-archive | 기본 24h | 면역 |
| 검색 | 채널 내 message search | 채널 내 post search + 태그 filter |
| 카탈로그화 | 시간순 | 태그·status·activity 정렬 가능 |
| 사용자 visible | thread list 깊게 들어가야 | 채널 상위 layout |

**과제 carry-over 보장** — 1주일 후 사용자가 "지난주 SKILL refactor 토론" 같은 검색 가능.

### 3.4 Slack 등가물 (v1.2.x)

v1.2.1+ Slack sub-version 에서:
- Forum equivalent = standard channel + thread-only enforcement (`posting_restricted_to_threads` 또는 application-level)
- 각 *과제 = thread 1개* (parent message 는 PM 의 task title + 1줄 summary)
- Tag = thread parent message 의 metadata (예: `<!-- tag: feat -->`) — application-level parsing
- Canvas (long-form 결정) = v1.2.4 슬롯

### 3.5 Bot Identity 옵션 정리

본 plan 의 *Bot Identity Model* 결정:

| 옵션 | v1.2.0 |
|---|---|
| (a) **별도 봇 × 5 (직원화)** | ✓ 채택 — Hermes V2 + Agno + Salesforce Slackbot + OpenClaw 합의 |
| (b) 단일 봇 + webhook persona (Hermes V1) | ✗ Hermes V2 가 약화 + Agent B 박제 ("mention trigger 안 함, intent filter 불가") |
| (c) 단일 봇 + LLM 내부 persona switching | ✗ context 폭증 (125KB 매 호출), 권한 단일 token 묶임 |
| (d) **별도 봇 + PM 발화 webhook overlay** | △ v1.2.x slot — Hermes V2 강조 약화로 *긴급도 낮음* |

### 3.6 Broadcast 채널 (v0.8.0 §3.6 v2 정합)

- 본 plan 영향 0 — broadcast 는 cross-user feed
- broadcast 발송 source = 각 사용자의 *forum post `solved` 태그 + decision.md*

---

## 4. Bot Identity Registry (Best Practice Q)

### 4.1 `<org>/.solosquad/bots.json` 스키마 (v1.1.0 정합)

```json
{
  "schema_version": 1,
  "bots": {
    "chief":    { "bot_user_id": "1234567890", "role": "supervisor", "tier": "leader", "skill_path": "<org>/agents/main/chief/SKILL.md" },
    "pm":       { "bot_user_id": "1234567891", "role": "main",       "tier": "leader", "skill_path": "agents/main/pm/SKILL.md",        "team": "product" },
    "engineer": { "bot_user_id": "1234567892", "role": "main",       "tier": "leader", "skill_path": "agents/main/engineer/SKILL.md",  "team": "engineering" },
    "designer": { "bot_user_id": "1234567893", "role": "main",       "tier": "leader", "skill_path": "agents/main/designer/SKILL.md",  "team": "design" },
    "marketer": { "bot_user_id": "1234567894", "role": "main",       "tier": "leader", "skill_path": "agents/main/marketer/SKILL.md",  "team": "marketing" }
  },
  "generated_at": "2026-05-27T15:00:00+09:00"
}
```

**v1.1.0 정합 사항:**
- 5 봇 = v1.1 의 5 main bot (chief + pm + engineer + designer + marketer). 20 specialist 는 별도 봇이 아님 — main bot 이 Task tool 로 spawn (composition.yaml 의 `members[]` 참조).
- `chief` 만 `role: supervisor` — 유일한 사용자 대면 봇 (v1.1 §5). 다른 4 main 은 `role: main` (Chief 의 dispatch 를 받음).
- `skill_path` 가 단순 dir 이 아니라 명시 경로 — chief 는 `<org>/agents/main/chief/SKILL.md` (org-customized), 나머지는 `agents/main/<name>/SKILL.md` (workspace bundle).
- `team` 필드 — chief 는 *팀에 속하지 않음* (4팀 위 supervisor). pm/engineer/designer/marketer 는 각각의 team 의 main.

**single source of truth** — 모든 봇이 startup 시 본 registry 로드 → peer 봇들의 `bot_user_id` 메모리 캐시. 다음 mechanism 의 1차 source:
- mention 라우팅 (§5)
- author-guard (§3.1)
- hop counter (§6.2)
- 9-hop diagnostic (§8)
- handoff trace (§9)

### 4.2 user.yaml 의 `bots` 필드 (v1.1.0 정합)

```yaml
handle: w1n
messenger: discord
bot_user_id_legacy: "1234567890"  # v0.8 single-bot ID, backward compat (이전 PM session 봇 ID, 이제 chief 가 같은 ID 사용)
bots:
  chief:
    bot_token_env: DISCORD_TOKEN_CHIEF
    bot_user_id: "1234567890"
    skill_path: "<org>/agents/main/chief/SKILL.md"
    role: supervisor
    tier: leader
  pm:
    bot_token_env: DISCORD_TOKEN_PM
    bot_user_id: "1234567891"
    skill_path: "agents/main/pm/SKILL.md"
    role: main
    tier: leader
    team: product
  engineer:
    bot_token_env: DISCORD_TOKEN_ENG
    bot_user_id: "1234567892"
    skill_path: "agents/main/engineer/SKILL.md"
    role: main
    tier: leader
    team: engineering
  designer:
    bot_token_env: DISCORD_TOKEN_DESIGNER
    bot_user_id: "1234567893"
    skill_path: "agents/main/designer/SKILL.md"
    role: main
    tier: leader
    team: design
  marketer:
    bot_token_env: DISCORD_TOKEN_MARKETER
    bot_user_id: "1234567894"
    skill_path: "agents/main/marketer/SKILL.md"
    role: main
    tier: leader
    team: marketing
```

> 구 `strategy` / `growth` / `experience` 팀명은 v1.1.0 에서 각각 *product* / *marketing* / *design* 으로 재편 (`docs/prd/v1.1-multi-agent-team-architecture.md` §3.3). user.yaml 의 `bots.<name>` 키도 같은 매핑.

### 4.3 Bot startup flow (v1.1.0 정합)

```
1. solosquad bot <name>          # 또는 solosquad bots (5 main bot 병렬)
2. process 진입점 (bin/solosquad.ts) — 신규 인자 <bot-name> 인식 (chief|pm|engineer|designer|marketer)
3. resolveBotIdentity(workspace, botName):
   3.1 user yaml `bots.<bot-name>` 읽기
   3.2 bot_token_env 의 환경변수 token 로드 (token precedence — Best Practice O)
   3.3 messenger gateway 연결 (Discord login / Slack socket)
   3.4 connected bot_user_id 가 yaml 의 bot_user_id 와 일치 검증 (mismatch 면 hard fail + actionable hint — Best Practice P)
4. loadBotIdentityRegistry() → 본 org 의 다른 봇 4명 ID 메모리 캐시
5. loadSkill(yaml.skill_path) — *자기 SKILL 만* (Best Practice T — SKILL isolation)
   - chief:    <org>/agents/main/chief/SKILL.md  (org-customized, 도메인 전문가화)
   - pm:       agents/main/pm/SKILL.md  (workspace bundle, 자율 product manager)
   - engineer: agents/main/engineer/SKILL.md  + composition.yaml.product team specialists
   - designer: agents/main/designer/SKILL.md  + composition.yaml.design team specialists
   - marketer: agents/main/marketer/SKILL.md  + composition.yaml.marketing team specialists
   - (specialist SKILL.md 는 main bot 이 Task tool 로 spawn 시 dynamic load — main bot startup 시 모두 미리 로드하지 않음)
6. subscribeChannels(yaml.channels.command, yaml.channels.works):
   - command: chief 만 listen (사용자 입력 진입점, v1.1 §5)
   - works: 모든 main bot listen, 단 `app_mention` 이벤트만 react (mention-only intent)
7. 헬스 ping 시작 — `<org>/memory/bot-health/<bot-name>.jsonl`
8. Chief 6+1 stage emission 활성화 — `<org>/memory/chief-stage-events.jsonl` 자동 기록 (v1.1 §5.2)
```

### 4.4 BotUserId branded type (Best Practice M 부분 흡수)

```typescript
// src/util/branded.ts (신규)
type Brand<K, T> = K & { __brand: T };
export type BotUserId = Brand<string, 'BotUserId'>;
export type GuildId   = Brand<string, 'GuildId'>;
export type ChannelId = Brand<string, 'ChannelId'>;

// 사용
function resolveBotIdentity(workspace: string, botName: string): {bot_user_id: BotUserId} {...}
```

→ Bot Identity Registry 의 모든 ID 가 type-level guard 받음. v1.0.2 의 charset mismatch (Discord username vs handle) 같은 *문자열 휴리스틱 회귀* 영구 방지.

전체 M 적용 (Channel/Guild/Sender 모두 branded) 은 v1.x 후속.

---

## 5. Mention Routing & DM Passthrough (Best Practice S)

### 5.1 Mention-only intent (main 봇, chief 제외)

**규칙:**
- 모든 main 봇 (pm / engineer / designer / marketer) 은 *자기 botUserId 가 mention 된 메시지에만 react*. Chief 는 #owner-command 채널의 모든 사용자 메시지에 listen (사용자 대면 진입점).
- Discord `app_mention` privileged intent / Slack `app_mention` event 만 subscribe
- 다른 봇/사용자 mention 메시지에는 *이벤트조차 받지 않음* (Discord) 또는 *받아도 무시* (Slack)

**예외:**
- Chief 봇은 `#command-<handle>` 의 *모든* 메시지를 listen (사용자가 PM 멘션 없이 자연 입력 가능)
- Chief 봇은 DM / `message.im` 도 listen (§5.3 cross-post)

### 5.2 구현 — discord-adapter.ts

```typescript
// src/messenger/discord-adapter.ts (v1.2 신규 라우팅)
client.on('messageCreate', (msg) => {
  const myBotId = this.identity.bot_user_id;
  // 1. self-message ignore (echo loop 가드 1)
  if (msg.author.id === myBotId) return;
  // 2. PM 만 #command 전체 listen
  if (this.identity.role === 'supervisor' && msg.channel.id === this.commandChannelId) {
    return this.routeToCommand(msg);
  }
  // 3. specialist 는 mention-only
  if (this.identity.role === 'specialist' && msg.mentions.users.has(myBotId)) {
    return this.routeToWorks(msg);
  }
  // 4. else: ignore (echo loop 방지)
});
```

### 5.3 DM passthrough (PM 만)

- Chief 봇이 Discord DM 받음 → 사용자 식별 → `#command-<handle>` 채널에 cross-post + 동일 thread 에서 응답
- 모바일 UX: 사용자가 *어디서든* PM 호출 가능, audit log 는 항상 채널에 유지
- 근거: Salesforce Slackbot mobile UX 패턴

### 5.4 Routing — Chief TRIAGE stage (v1.1 §5.2 정합)

PM 의 Task dispatch 결정:
1. 1차 deterministic — SKILL frontmatter v2 의 `triggers.keyword` + `triggers.slash` 매칭 (v1.1 §10 frontmatter v2). 본 plan 작성 시 가정했던 `domain_tags` / `routing_keywords` 명명은 v1.1.0 의 `triggers` 로 통합됨.
2. 2차 LLM fallback — 후보 0/2+ 시

**v1.2 의 메신저 측 기여:** mention parser 가 사용자 메시지의 `@<team-bot>` 명시적 mention 처리 (예: 사용자가 `@eng 이거 처리해`) → routing 의 0차 (명시 우선)

### 5.5 PM 단일 voice + cite

- 사용자가 *보는 봇 = PM 만* (`#command` 안)
- PM 이 specialist 회신을 *직접 paste 안 함* → 요약 + cite:
  > "growth 가 가설 3개 제안: (1)... (2)... (3)... — [forum post link]"
- 사용자 디버깅 가능 (link 클릭 → 원본 specialist 메시지)

---

## 6. Safety Net — Echo Loop / Hop / Token Budget

### 6.1 Echo loop 3중 가드 (Best Practice S 확장)

**위험:** 봇 A 가 봇 B 멘션 → 봇 B 응답에 봇 A 멘션 포함 → 무한 (AnythingLLM/llmcord/Botpress 사례)

**3중 방어:**

| # | 가드 | 작동 |
|---|---|---|
| 1 | Self-author guard | 봇은 자기 botUserId 의 메시지에 절대 react 안 함 (§5.2 step 1) |
| 2 | Bot-author rate-limit | 다른 봇의 멘션에는 *forum post 별 rate limit* (10 mentions / 10분) |
| 3 | Content-hash dedup | 동일 forum post 안에서 `(sender_bot, sha256(content)[:8])` 가 30초 내 재발생 시 무시 (idempotency) |

### 6.2 Hop counter (Best Practice V — Composable Termination Algebra)

- 모든 forum post 시작 시 `hop_count = 0`
- PM → specialist 멘션 = `hop_count++`
- specialist → PM 회신 = `hop_count++`
- **max `hop_count` = 6** (PM → spec → PM → spec → PM → close)
- 초과 시 Chief 봇이 *"hop budget exhausted — synthesizing and closing thread"* 메시지 + 사용자 cross-post 후 강제 종료

**근거:** Anthropic engineering "circuit breaker" + LangGraph `recursion_limit` 기본 ~20 의 *축소판* (channel 환경은 latency × LLM cost × token 더 비쌈)

**구현 위치:** `<org>/memory/handoff-trace.jsonl` (§9) 의 hop record append-only.

### 6.3 Thread token budget (Best Practice R)

- 각 forum post 누적 토큰 ceiling:
  - prompt + completion 합 **200,000 tokens**
  - 메시지 수 ceiling **50개**
- 누적은 `handoff-trace.jsonl` 합산
- 초과 시 hop counter 동일하게 PM 강제 종료 + 사용자 alert

**근거:** Anthropic 자체 측정 — multi-agent = single chat × **15배 토큰**. "Cut the Crap" 논문 2-11.8배. → SoloSquad 가 본 사용자 cost 폭증 막는 *유일한 방어선*.

### 6.4 Composable Termination Algebra (Best Practice V)

forum post 종료 조건:
```
TERMINATE ⟺ (hop_count ≥ 6)
         OR (token_budget ≥ 200_000)
         OR (message_count ≥ 50)
         OR (user_says "stop" | "취소" | "중단")
         OR (PM emits "DONE" | "RESOLVED")
         OR (forum tag changes to "solved")
```

→ AutoGen v0.4 의 `MaxMessage AND TokenUsage AND/OR` 합의 정합.

---

## 7. Forum Channel 구현 (Discord)

### 7.1 ensureChannels 확장 (v1.0.3 §13.6.16 정합)

- 기존 (v1.0.3): `ensureChannels` 가 카테고리 `solosquad` + 각 user 의 `#command-<handle>` + `#works-<handle>` standard channel 생성
- v1.2: `#works-<handle>` 를 **Forum Channel type** 으로 생성
  - 신규 사용자: 자동 Forum 으로
  - 기존 사용자 (v1.0.x 의 standard channel `#works-<handle>` 보유): **migration script** 가 *경고만* + 사용자 수동 변환 안내 (Discord API 가 channel type 변환 안 함)
  - 또는 자동으로 *새 Forum 채널 생성* + 기존 `#works-<handle>` 를 `#works-<handle>-archive` 로 rename

### 7.2 Forum tag 등록

```typescript
// src/messenger/discord-adapter.ts (v1.2 신규)
const REQUIRED_TAGS = ['bug', 'feat', 'refactor', 'docs', 'design', 'review', 'urgent', 'experiment', 'solved'];

async function ensureForumTags(forumChannel: ForumChannel) {
  const existing = forumChannel.availableTags.map(t => t.name);
  const missing = REQUIRED_TAGS.filter(t => !existing.includes(t));
  if (missing.length > 0) {
    await forumChannel.setAvailableTags([
      ...forumChannel.availableTags,
      ...missing.map(name => ({ name, moderated: false }))
    ]);
  }
}
```

### 7.3 Forum post 생성 — Chief Supervisor (v1.1 §5)

Chief 6+1 stage 의 DECOMPOSE → DISPATCH 시점에 forum post 생성. PM 은 사용자와 직접 대화하지 않으므로 forum post 작성도 Chief 가 wrap:

```typescript
// Chief Supervisor (v1.1 §5.2 6+1 stage 정합)
async function createForumPost(task: Task) {
  const post = await forumChannel.threads.create({
    name: task.title,                                   // "signup-cvr-2026-q3"
    appliedTags: [tagForCategory(task.skill_category)], // [planning] 등 v1.1 §9.1 category
    message: {
      // Chief 가 PM/engineer/designer/marketer 중 dispatch 대상 main bot 들을 mention.
      // specialist 단위 mention 은 X — main bot 이 자기 팀의 composition.yaml 보고 spawn.
      content: `[Brief] ${task.summary}\n@marketer @designer ... 부탁`,
    },
  });
  await persistForumPostUrl(task.workflow_id, post.url);
  // Chief 가 chief-stage-events.jsonl 에 DISPATCH stage emit (v1.1 §5.2)
  await emitStage({ turn_id, stage: "DISPATCH", task_id: task.id, dispatched: task.main_bots });
  return post;
}
```

### 7.4 workspace.yaml `forum_channels` 옵션 (신규)

```yaml
# workspace.yaml
forum_channels:
  enabled: true                # v1.2 default
  auto_resolve: true           # works-<handle> 자동 Forum 타입 확정
  required_tags:               # 사용자 추가 가능
    - bug
    - feat
    - refactor
    - docs
    - design
    - review
    - urgent
    - experiment
    - solved
```

---

## 8. 9-hop Binding Diagnostic (Best Practice P 전체 적용)

### 8.1 v1.0.4 의 5-hop → v1.2 의 9-hop 일반화

v1.0.4 commit `32c0dde` 의 `diagnoseProductByGuildFailure` 가 *single-bot* 환경의 5-hop binding chain 진단. v1.2 의 *multi-bot* 환경은 4 hop 추가:

| Hop | 진단 항목 | 메시지 |
|---|---|---|
| 1 | Bot Token loaded | "bot.<name> token not in env (key: <bot_token_env>)" |
| 2 | Bot Identity verified | "bot.<name> connected bot_user_id <id> ≠ user.yaml.bots.<name>.bot_user_id <expected>" |
| 3 | ownOrgSlug resolved | (v1.0.4 hop 3) "bot has no resolved org. user.yaml matching failed" |
| 4a | config.yaml present | (v1.0.4 hop 4a) "config.yaml missing at <path>" |
| 4b | guild_id stamped | (v1.0.4 hop 4b) "no guild_id field in config.yaml" |
| 4c | guild_id matches | (v1.0.4 hop 4c) "config.yaml.guild_id=<a> but message came from guild <b>" |
| 5 | loadProducts includes org | (v1.0.4 hop 5) "loadProducts() does not include org=<slug>" |
| **6** | Bot Identity Registry loaded | "bots.json registry not loaded for org=<slug>" |
| **7** | Peer bot lookup | "bot.<peer-name> not found in bots.json. mention failed" |
| **8** | Forum channel exists | "#works-<handle> not a Forum channel (Discord) or thread-only not enforced (Slack)" |
| **9** | Mention permission | "bot lacks Message Content Intent (Discord) or `app_mention` scope (Slack)" |

### 8.2 구현 — `diagnoseBindingFailure` helper

v1.0.4 의 `diagnoseProductByGuildFailure` 를 *generic* 화:

```typescript
// src/messenger/discord-adapter.ts
function diagnoseBindingFailure(ctx: BindingContext): { hop: number; message: string; hint: string } {
  // hop 1
  if (!ctx.bot_token) return { hop: 1, message: `bot.${ctx.bot_name} token not in env (key: ${ctx.bot_token_env})`, hint: `Set DISCORD_TOKEN_${ctx.bot_name.toUpperCase()} env var. See solosquad doctor --bots` };
  // hop 2
  if (ctx.connected_bot_user_id !== ctx.expected_bot_user_id) return { hop: 2, message: `bot.${ctx.bot_name} connected bot_user_id ${ctx.connected_bot_user_id} ≠ expected ${ctx.expected_bot_user_id}`, hint: `Update user.yaml or re-create bot. See solosquad doctor --bots` };
  // hop 3~5 (v1.0.4 existing)
  // ...
  // hop 6
  if (!ctx.bots_registry_loaded) return { hop: 6, message: `bots.json registry not loaded for org=${ctx.org_slug}`, hint: `Run solosquad bots to regenerate registry` };
  // hop 7
  if (!ctx.peer_bot_found) return { hop: 7, message: `bot.${ctx.peer_bot_name} not found in bots.json. mention failed`, hint: `Add peer bot to user.yaml.bots and run solosquad bots` };
  // hop 8
  if (!ctx.forum_channel) return { hop: 8, message: `#works-${ctx.handle} not a Forum channel`, hint: `Re-run solosquad init or create Forum manually` };
  // hop 9
  if (!ctx.mention_intent) return { hop: 9, message: `bot lacks Message Content Intent`, hint: `Enable in Discord Developer Portal: https://discord.com/developers/applications` };
  // success
  return { hop: 0, message: 'binding success', hint: '' };
}
```

### 8.3 actionable hint — 모든 hop

Best Practice P 전체 적용 — 모든 hop 에 *어디부터 디버깅* 명시. `solosquad doctor --bots` 가 본 helper 호출.

---

## 9. Handoff Trace (Best Practice Z)

### 9.1 `<org>/memory/handoff-trace.jsonl` 스키마

각 hop = 1 record (append-only):

```jsonl
{"ts": "2026-05-24T15:00:01+09:00", "hop": 1, "parent": "user:w1n", "child": "bot:pm", "task_id": "signup-cvr-2026-q3", "forum_post_url": "https://discord.com/...", "input_summary": "회원가입 conversion 개선", "output_summary": "growth+design 멘션", "tokens_in": 142, "tokens_out": 87, "duration_ms": 1240, "exit_reason": "dispatched"}
{"ts": "...", "hop": 2, "parent": "bot:pm", "child": "bot:growth", "task_id": "signup-cvr-2026-q3", ...}
```

### 9.2 활용

- **회귀 테스트:** v1.0.4 의 5-hop diagnostic 가 *one-off log* 였다면, v1.2 는 *append-only 영구* — pre/post v1.2 비교 가능
- **토큰 비용 대시보드:** v1.x-dashboard-interaction (cascade-shifted) 의 source
- **Hop counter / token budget enforcement** — §6.2/§6.3 의 누적 source
- **Magentic Task Ledger 와 보완** — handoff-trace 는 *event-level*, ledger 는 *state-level*

### 9.3 trace-rotate routine (신규)

- cron daily 02:00 — `schedules/bot-health-check.md` (v1.1.0 신규 schedule, PRD §12.2)
- 7일 hot (`handoff-trace.jsonl`) + 30일 FTS5 archive (`<org>/memory/archive.sqlite` 의 `handoff_trace` table)
- v0.6 archive-rotate 패턴 동일

---

## 10. v1.2.x — Slack 분리

### 10.1 v1.2.0 vs v1.2.x 분리 이유

- Discord = v1.0.x 4 hotfix 시리즈의 1차 검증 messenger (본 사용자 실사용)
- Slack = v0.8.0 multi-user messenger 이후 *adapter 만 동작* — multi-bot collab 미검증
- 본 plan 의 모든 spec (Bot Identity Registry · Forum Channel · 9-hop · trace.jsonl) 은 *messenger-agnostic* 이지만 Slack 측 구현은 별도 검증 사이클 필요

### 10.2 Slack 측 차별점

| 차원 | Discord (v1.2.0) | Slack (v1.2.x) |
|---|---|---|
| Forum channel 등가물 | Discord Forum Channel (네이티브) | standard channel + thread-only enforcement |
| Mention intent | `app_mention` privileged intent | `app_mention` event subscription |
| DM passthrough | Discord DM | `message.im` event |
| Tag-based routing | Forum tag (네이티브) | thread parent message 의 metadata |
| Multi-bot app 등록 | Developer Portal × 5 | Slack App Directory × 5 |
| Long-form 결정 박제 | Forum post 의 첫 메시지 stickied | Canvas (v1.2.4 슬롯) |

### 10.3 v1.2.x slot candidates

| 버전 | 내용 |
|---|---|
| v1.2.1 | Slack Forum equivalent (thread-only enforcement application-level) |
| v1.2.2 | Slack `app_mention` + `message.im` 정합 |
| v1.2.3 | Slack tag-based routing (metadata-driven) |
| v1.2.4 | Slack Canvas + thread (long-form 결정 박제) |
| v1.2.5+ | Webhook persona overlay (옵션 d) — Discord 도 함께 |

(v1.2.x 분할은 v1.2.0 publish 후 1~2 분기 사용자 데이터 보고 결정)

---

## 11. v1.2 신설 4 Best Practice (메신저 layer, v1.0.4 L~P 후속)

본 plan 은 *메신저 layer* (Q, R, S, U — 4건) 채택. v1.1 의 *협업 모델 layer* (T, V, W, X, Y, Z — 6건) 과 합쳐서 v1.1+v1.2 의 *10 Best Practice* 완성.

| # | Best Practice | 출처 | 본 plan 적용 위치 |
|---|---|---|---|
| **Q** | Bot Identity Registry — single source of truth | OpenClaw accountId routing + Agno `agent_id` 매핑 | §4 `<org>/.solosquad/bots.json` |
| **R** | Thread Token Budget + Circuit Breaker | aisecuritygateway.ai + Anthropic agent loop | §6.3 (200k / 50 msgs per forum post) |
| **S** | Mention-only Intent + DM Passthrough | Discord `app_mention` privileged-intent + Slack | §5.1 specialist mention-only, §5.3 PM DM passthrough |
| **U** | Forum Channel + Tag Routing | Discord Forum FAQ + accord.gg + metacrm | §3.2 `#works-<handle>` = Forum + §7 7-tag 등록 |

---

## 12. Out of scope

- **L2~L5 에이전트 팀 + 워크플로우 + Goal + Routine + 실험 인프라** — **v1.1 plan** 으로 위임
- **Webhook persona overlay (옵션 d)** — v1.2.x sub-version (Hermes V2 약화로 긴급도 낮음)
- **Best Practice L (페어링 코드 + approve CLI)** — v1.2.x 후속 (multi-bot 환경 별도 설계)
- **WhatsApp / iMessage / Telegram 등 18-platform 확장** — v1.x post-launch
- **enterprise SSO · multi-tenant** — boundary 외
- **자동 머지** — v0.8.2 박제 (영구 거부)
- **deployment model 재설계** (SaaS host + 웹 대시보드 OAuth click) — v1.x-dashboard-interaction (cascade-shifted)

---

## 13. 구현 체크리스트

### 13.1 신규 CLI 명령 (api-stability §3 — minor)

| 명령 | 책임 |
|---|---|
| `solosquad bot <name>` | 봇 1개 startup (기존 `solosquad bot` 의 다중 봇 분기) |
| `solosquad bots` | 5 main bot (chief + pm + engineer + designer + marketer) 병렬 startup + Bot Identity Registry 자동 생성/갱신 |
| `solosquad doctor --bots` | 9-hop binding diagnostic 전체 봇 실행 |

### 13.2 schema 변경 2건 (api-stability §2 — minor, add only)

| Schema | 변경 |
|---|---|
| `user.yaml` | 신규 `bots:` 필드 (§4.2). 기존 `bot_user_id` 는 `bot_user_id_legacy` 로 backward compat |
| `workspace.yaml` | 신규 `forum_channels:` 옵션 (§7.4) — Discord forum channel auto-resolve + required tags |

### 13.3 작업 단위

| # | 작업 | 우선순위 | 의존 | split 후보 |
|---|---|---|---|---|
| 1 | Bot Identity Registry (`bots.json`) 스키마 + loader | high | v1.1 (bot_name in SKILL frontmatter) | v1.2.0 |
| 2 | `solosquad bots` CLI + 5 봇 병렬 startup | high | #1 | v1.2.0 |
| 3 | `#works-<handle>` Forum Channel 생성 + tag 등록 | high | Discord adapter | v1.2.0 |
| 4 | Mention-only intent — discord-adapter 라우팅 v1.2 | high | #1, #3 | v1.2.0 |
| 5 | Hop counter + Thread token budget enforcement | high | #4 | v1.2.0 |
| 6 | handoff-trace.jsonl + trace-rotate routine | high | #4 | v1.2.0 |
| 7 | 9-hop `diagnoseBindingFailure` helper | high | #1, #4 | v1.2.0 |
| 8 | `BotUserId` branded type (M 부분 흡수) | medium | #1 | v1.2.0 |
| 9 | Application-level author-guard (#command 보호) | medium | v1.0.2 정신 | v1.2.0 |
| 10 | Echo loop 3중 가드 (self + rate-limit + dedup) | medium | #1 | v1.2.0 |
| 11 | DM passthrough cross-post | medium | #4 | v1.2.0 |
| 12 | bot-health-check 확장 — 5 main bot heartbeat aggregation (v1.1.0 `schedules/bot-health-check.md` base) | medium | #1 | v1.2.0 |
| 13 | v1.0.4 G + H + P 흡수 정합 (커밋 보존, 그 위 적용) | (publish 게이트) | v1.0.4 commits | v1.2.0 |
| 14 | Slack Forum equivalent (thread-only) | low | v1.2.0 검증 후 | **v1.2.1** |
| 15 | Slack `app_mention` + `message.im` | low | #14 | **v1.2.2** |
| 16 | Webhook persona overlay (옵션 d) | low | v1.2.0 안정화 후 | **v1.2.5+** |

### 13.4 Pre-publish 게이트 (git-workflow.md 4-docs 룰 정합)

- [ ] `npm test` — 신규 catcher (Bot Identity Registry 9-hop · mention-only · Forum Channel · Echo guard · Hop counter)
- [ ] `npx tsc --noEmit` clean
- [ ] `npm run docs-check` — 4 docs (`product-roadmap.md`, `architecture.md`, `master-guide_{ko,en}.html`) 가 `1.2.0` 멘션 확인
- [ ] cascade-rename 검증 (잔여 v1.1 cross-ref grep)
- [ ] `npm publish --dry-run` clean
- [ ] 수동 검증:
  1. 5 봇 token 발급 (Discord Developer Portal × 5)
  2. `solosquad bots` 실행 → 5 봇 동시 startup + Bot Identity Registry 자동 생성
  3. `#command-w1n` 메시지 → Chief 봇 응답 → forum post 생성 (`#works-w1n`)
  4. forum post 안 `@growth @design` 멘션 → 두 봇 회신
  5. hop counter / token budget 동작 확인 (의도적 6 hop 초과 메시지 시퀀스)
  6. `handoff-trace.jsonl` append 확인
  7. 9-hop diagnostic — 의도적 misconfig (예: bot token 1개 unset) → actionable hint 확인

---

## 14. Backward Compatibility

### 14.1 v1.1 사용자

| 사용자 시나리오 | v1.2 영향 |
|---|---|
| `solosquad update` 실행 | npm 1.1.0 → 1.2.0 |
| `solosquad migrate --apply` | 1.1.0 → 1.2.0 chain |
| 기존 단일 봇 (v1.0.x ~ v1.1.0 까지 chief 봇만 동작) | `user.yaml.bots.chief` 자동 채움 — *최소한 Chief 봇만 동작* 보장 (v1.0.x PM session 의 같은 ID 재사용). pm / engineer / designer / marketer 4 main 봇의 token 발급은 사용자 후속 |
| 기존 `#works-<handle>` (standard channel) | migration 경고 + 사용자 수동 변환 안내 (또는 `-archive` 자동 rename + 새 Forum 채널 생성) |
| 기존 `#workflow` 채널 (v0.2.4) | 폐기 없음 — 그대로 살아있되 `#works-<handle>` 으로 1차 사용 |
| 기존 forum channel 없는 사용자 | v1.2 봇 startup 시 자동 생성 (v1.0.4 의 config.yaml auto-create 동일 정신 + Best Practice P 9-hop diagnostic) |

### 14.2 v1.0.3 사용자 (이전 npm latest)

- npm 1.0.3 → 1.1.0 → 1.2.0 chain (v1.0.4 publish skip)
- workspace.yaml.version migration: 1.0.3 → 1.1.0 → 1.2.0 (각 단계 idempotent)
- v1.0.4 의 Discord config.yaml auto-create 도 함께 적용 (commit `c990cf3` 보존)
- Slack 사용자: v1.2.0 시점은 *변경 0*. v1.2.1+ 부터 적용

### 14.3 다운그레이드 (v1.2 → v1.1)

- 자동 작성된 `bots.json` 무손상 — v1.1 reader 가 *무시*
- `user.yaml.bots` 필드 무손상 — 1.1 reader 가 *unknown field* 로 ignore
- `#works-<handle>` Forum Channel — 1.1 reader 가 standard channel 처럼 listen (단 PM 의 forum-post-create 불가능)
- handoff-trace.jsonl 무손상

---

## 15. 변경 이력 / 레퍼런스

### 15.1 변경 이력

- **2026-05-27 (현재)**: v1.1.0 출시 (`main` 병합 commit `58a25ed`) 반영. §0 신설 — v1.1.0 출시 후 본 plan 가정 7건 갱신 (`bot_name` → `tier`/`team`/`used_by`, "5 코어 봇" → "5 main bot", PM session → Chief, `assets/orchestrator/` → `<org>/agents/main/chief/`, `assets/agents/{team}/` → `agents/specialists/` + `composition.yaml`). Bot Identity Registry §4.1 / §4.2 / §4.3 의 5 bot 정의를 v1.1 의 chief + pm + engineer + designer + marketer 로 재작성 (skill_path 명시, role=supervisor|main, tier=leader, team 필드). §5 Mention routing — Chief 가 `#owner-command` 진입점, 4 main 봇은 mention-only intent. §7.3 Forum post 생성 흐름을 Chief 6+1 stage (DECOMPOSE → DISPATCH) 와 정합화. §11 schedule 참조를 `schedules/bot-health-check.md` (v1.1 §12.2 신규) 로 갱신.
- **2026-05-24**: 최초 작성. mega-plan `v1.1-multi-agent-messenger-collaboration.md` 를 *v1.1 (에이전트 팀 + 워크플로우)* + *v1.2 (메신저)* 두 plan 으로 분리. 본 plan 은 v1.0.4 G+H+P+L+M+N+O 흡수 + 14 reference + Hermes V2 §3.3 (Webhook 약화) 반영 + 5-hop → 9-hop 일반화.

### 15.2 내부 의존 (참조 docs)

- `docs/prd/v1.1-multi-agent-team-architecture.md` — **hard dependency, 출시 완료** (5 main bot 직원화 + SKILL frontmatter v2: `tier`/`team`/`category`/`used_by`/`collaborators`/`skills_used`/`pm_conventions`)
- `docs/prd/v1.0-official-launch.md` — api-stability 시작점
- `docs/prd/v1.0.1-discord-ready-deprecation.md` §2.4 — `@<slug>` mention parser
- `docs/prd/v1.0.2-discord-author-guard-decoupling.md` — author-guard decoupling 정신
- `docs/prd/v1.0.3-discord-triple-bug-fix.md` — guild-org binding · ensureChannels · category
- `docs/prd/v1.0.4-messenger-config-auto-create.md` — G+H+P 흡수 source. v1.2 publish 후 *삭제 결정* (commit 보존, plan doc 만)
- `docs/prd/v0.8-multiuser-messenger.md` — N:1 봇 모델

### 15.3 외부 reference

#### 14 reference (binding + multi-bot collab)

OpenClaw · Claude Code Channels · LangChain · AutoGen (Composio) · Composio MCP · llmcord · openai/gpt-discord-bot · LibreChat-DiscordBot · AnythingLLM · Agno (Phidata) · Microsoft Agent Framework · LangGraph Supervisor · OpenAI Agents SDK · Salesforce Slackbot MCP

#### 핵심 4건 (본 plan 의 입력)

- `docs/ideation/multi-agent-directory.md` V2 — L1 Gateway 의 *Webhook persona 약화* 박제
- `docs/ideation/AI_Agent_Harness_Report.md` — MCP 호환 + 가드레일 3단계 (§2.2)
- `docs/ideation/2026-05-23-multi-agent-v1.1-synthesis.html` — 6 개선 제안 + 10 BP 매트릭스 + 9-hop diagnostic 박제
- `docs/prd/v1.0.4-messenger-config-auto-create.md` §7.2 — 9-reference 조사 + L+M+N+O+P 5 BP

#### 신규 발굴 (2026-05-24)

- **Mastra / Vercel AI SDK Agents** — TypeScript multi-agent 표준 (deployment model 재설계 시 reference, v1.x-dashboard 슬롯)
- **nanobot (HKUDS)** — OpenClaw 의 4k lines 경량 Python fork (MCP first-class 패턴 차용 가능)

---

## Appendix A · 신규 파일 / 디렉토리 / 환경변수 list

### 신규 파일
- `<org>/.solosquad/bots.json` — Bot Identity Registry (§4.1)
- `<org>/memory/handoff-trace.jsonl` — Structured handoff trace (§9.1)
- `<org>/memory/bot-health/<bot-name>.jsonl` — bot health log (§4.3, routine 은 v1.1 §10.1)

### 신규 환경변수 (5 봇 × token + intent)
- `DISCORD_TOKEN_PM`, `DISCORD_TOKEN_ENG`, `DISCORD_TOKEN_DESIGN`, `DISCORD_TOKEN_GROWTH`, `DISCORD_TOKEN_STRATEGY`
- (Slack 등가물 v1.2.x): `SLACK_BOT_TOKEN_PM`, ... (5개)

### 신규 SKILL.md frontmatter 영향
- v1.1 의 `bot_name` 필드가 본 registry 의 *어느 봇 소속* 결정 (v1.2 자체는 SKILL frontmatter 변경 0)

### 신규 workspace.yaml 옵션
- `forum_channels:` (§7.4) — enabled / auto_resolve / required_tags

### 신규 user.yaml 필드
- `bots:` (§4.2) — 5 main bot (chief / pm / engineer / designer / marketer) 별 bot_token_env + bot_user_id + skill_path + role + tier + team (chief 제외)

---

**문서 길이:** ~670 lines.

**다음 단계:**
1. 본 plan 검토 — 사용자 ack 후 commit (`plan(v1.2): messenger connection — discord first + slack v1.2.x split + absorb v1.0.4 G+H+P+L+M+N+O`)
2. v1.1 plan + v1.2 plan 두 doc commit 후 기존 `v1.1-multi-agent-messenger-collaboration.md` (untracked, ~960줄 mega) 삭제
3. `v1.x-workflow-goal-routine-evolution.md` → `v1.x-archive-workflow-goal-routine-evolution.md` rename
4. `v1.0.4-messenger-config-auto-create.md` *(commit 보존, doc 만 삭제)* — v1.2 §0 + §8 흡수 완료
5. 4-docs 갱신 (product-roadmap · architecture · master-guide ko/en) — v1.0.4 entry 제거 + v1.1 + v1.2 entry 추가
