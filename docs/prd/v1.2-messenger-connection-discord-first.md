# v1.2 — Messenger Connection (Chief on Discord, auto-connect first)

> **핵심 1줄:** 조직 1개당 Discord 봇 1개 (= v1.1 Chief). 사용자는 Chief 와 자연어 대화 → Chief 가 6+1 stage 으로 과제화 / 오케스트레이션 / 보고. 자동 연결 최대화 — Discord 정책상 manual 인 단계만 사용자가 click.
>
> **v1.1 과의 정합:** v1.1.0 의 `<org>/agents/main/chief/SKILL.md` + `src/bot/chief-runner.ts` + `<org>/memory/chief-stage-events.jsonl` 가 **이미 사용자 대면 single agent** 임. v1.2 는 그 single agent 를 Discord 표면에서 *살아 움직이게* 한다 — multi-bot 분기 없음.

**로드맵 위치:** v1.1.0 ✓ 출시 (multi-agent team) → **v1.2 본 plan (Discord 자동 연결)** → v1.2.x (Slack sub-version) → v1.x (대시보드 · 지식 온톨로지 · LLM backend)

**의존:**
- **v1.1.0** (Chief / 4 main / 20 specialist 구조, chief-runner, chief-stage-events, open_questions[]) — *hard dependency, 출시 완료*
- v0.8.0 (`<org>/.solosquad/users/<handle>.yaml`, `resolveBotIdentity`, `command-<handle>` / `works-<handle>` 채널 페어)
- v1.0.1 (`@<slug>` mention parser)
- v1.0.3 (Discord guild-org binding via `ownOrgSlug`, ensureChannels, category lookup)
- v1.0.4 (Discord `config.yaml` load-or-empty + 5-hop diagnostic)

**기준 버전:** v1.2.0 (Discord) → v1.2.1+ (Slack sub-version). 신규 인프라 3건 (OAuth Invite URL generator + guildCreate onboarding flow + thread-per-workflow), 신규 CLI 1건 (`solosquad discord invite-url`), schema 변경 1건 (`workspace.yaml.messenger.discord.install_mode`).

**분류:** Minor (semver). v1.1.0 의 *내부 에이전트 격상* 위에 *외부 가시 UX 확장* 만 추가. CLI freeze 침범 0, schema breaking 0.

---

## 0. v1.1.0 → v1.2 narrative 정합

v1.2 작성 초안 (2026-05-24) 은 *5 코어 봇 직원화* 가정으로 작성. 2026-05-27 directive 로 **scope 축소**: 5 봇 → 1 Chief. 이유:

1. **사용자 인지 부담** — 1인 솔로 founder 가 Discord 에 5 봇 분리 인지 / 관리 (5 토큰 발급, 5 invite URL, 5 verification 추적) 는 본 제품 톤(*"24/7 자율 팀"*)과 충돌.
2. **v1.1 의 Chief 가 이미 supervisor** — 4 main + 20 specialist 는 Task tool 로 내부 spawn. Discord 표면에서 *외부 가시 봇* 은 Chief 1개로 충분.
3. **Discord 정책 친화** — verified-app 한도 75 길드 (1 봇 기준), 5 봇이면 봇마다 별도 verification + intent 토글 + 사용자 mention 학습 부담.
4. **회귀 위험 축소** — 5 봇 환경의 mention 라우팅 / echo loop / hop budget 은 multi-bot 일 때만 발생하는 함정. 1 봇 = 0 회귀.

**의도적 폐기 (5-bot 초안의 부분):**
- 별도 봇 × 5 (chief/pm/engineer/designer/marketer) — 보류. 모두 Chief 1개 봇 위에서 Task tool 호출.
- Bot Identity Registry `bots.json` 5-entry 스키마 — 단일 Chief identity 만 user.yaml 에 박제 (구 v0.8.0 single-bot 모델과 동일 인터페이스).
- 9-hop diagnostic — 5-hop 으로 환원 (v1.0.4 P 와 동일).
- Forum Channel topology 강제 — text channel + dynamic thread (gpt-discord-bot 패턴) 로 단순화.

이 결정의 trade-off:
- **잃는 것:** 메신저 표면에서 "팀이 보인다" 효과. → 그러나 v1.1 의 `works-<handle>` 채널 + Chief 가 dispatch 결과를 `[engineer] backend-engineer 가 X 했음` 식으로 narrate 하면 동등 효과.
- **얻는 것:** onboarding 5분 → 1분, 회귀 면적 80% 감소, Discord 100길드 verification skip.

---

## 1. 핵심 질문 & 성공 기준

### 1.1 v1.2 가 답해야 할 7 질문

| # | 질문 | 본 plan 답 |
|---|---|---|
| 1 | Chief 봇 = 사용자 공식 봇 vs SoloSquad 공식 봇 중 어디가 default? | §3 — **사용자 own bot** (BYO) default. v1.0.x 흐름 유지, 사용자 통제 + 100길드 한도 무관. 단, OAuth Invite URL 생성으로 사용자 manual 최소화 |
| 2 | OAuth Install URL 자동 발급은 어떻게? | §3.1 — `solosquad discord invite-url` 가 user yaml + Discord application client_id 로 URL string 합성. browser 자동 open (실패 시 clipboard fallback) |
| 3 | Discord 정책상 자동화 불가능한 manual step 은? | §3.3 — Application 생성 / 토큰 발급 / MESSAGE_CONTENT intent 토글 / OAuth click "Authorize" 4건. 그 외 모두 자동 |
| 4 | 첫 guild join 시 Chief 가 어떻게 자기 소개하나? | §5 — `guildCreate` → systemChannel 에 onboarding embed + 2 button ("Auto-create channels" / "I'll choose manually"). 사용자 click 시 채널 생성 + Chief 가 `command-<handle>` 에서 첫 대화 시작 |
| 5 | 사용자 ↔ Chief 대화의 컨텍스트는 어떻게 유지? | §7 — single message → 짧은 대화 (`#command` 채널 내부). 장기 workflow → Chief 가 자동 thread 생성 (`message.startThread`). 1 workflow = 1 thread |
| 6 | Chief 가 sub-agent 진행 상황을 사용자에게 어떻게 보고? | §8 — chief-stage-events.jsonl 의 stage transition 이 thread 내 메시지로 자동 projection. *"[engineer] backend-engineer 가 PR #42 생성"* / *"[pm] 가설 confidence 78"* 등 narrative 형식 |
| 7 | 1 봇 = 100 길드 한도 vs 솔로 founder 1 길드 시나리오 | §3.4 — 한도 내. SoloSquad 공식 봇 운영 모드 (v1.x slot) 추가 시 verification 필요. 본 v1.2 BYO mode 는 사용자 personal app, 한도 무관 |

### 1.2 성공 기준 (v1.2.0)

- [ ] 신규 사용자: `solosquad init` → Discord Developer Portal 화면 안내 (가이드 링크 + 토큰 prompt) → `solosquad discord invite-url` 출력 → 사용자 click 1회 → 채널 자동 생성 → `#command-<handle>` 에서 Chief 첫 메시지. **5분 이내**.
- [ ] 기존 v1.1.0 사용자: `solosquad migrate --apply` 1회로 1.2.0 으로 무손상. Discord 채널 / token / org binding 모두 보존.
- [ ] Chief 6+1 stage 가 Discord thread 에 narration 으로 표출 (TRIAGE → ... → RETROSPECT). 사용자가 "지금 뭐 하고 있어?" 물으면 현재 stage 명시.
- [ ] `solosquad doctor --discord` 가 5-hop diagnostic (token / intent / OAuth / guild binding / channel ACL) 통과 / 실패 항목 명시.
- [ ] 사용자가 명시적으로 봇을 새 서버에 다시 초대 → 봇이 자동으로 채널 추가 생성 + Chief 자기 소개 (`guildCreate` 흐름 멱등).

---

## 2. 외부 reference 합성

본 plan 은 *조사 보고서* (2026-05-27 reference research) 의 권장 사항 통합. 핵심 reference 6건:

| Source | URL | 채택 |
|---|---|---|
| **llmcord** (jakobdylanc) | https://github.com/jakobdylanc/llmcord | @mention + reply chain context, message LRU cache, DM 도 연속 대화. → §7.3 |
| **openai/gpt-discord-bot** | https://github.com/openai/gpt-discord-bot | `/chat` slash 가 public thread 생성 → thread 전체 = conversation context, 한계 도달 시 auto-close. → §7.4 |
| **Composio Discord toolkit** | https://docs.composio.dev/toolkits/discordbot | agent → Discord 도구 호출 추상화 (메시지 / role / 채널). 본 plan 의 Chief → discord-adapter API 설계 참고. → §8 |
| **Discord 공식 OAuth2 docs** | https://discord.com/developers/docs/topics/oauth2 | `scope=bot+applications.commands` + permissions bitfield 산정. → §4 |
| **discord.js v15 가이드** | https://discordjs.guide/popular-topics/intents | GatewayIntent 의 3-tier (general / privileged / restricted) — MESSAGE_CONTENT 의존도 최소화. → §3.3 |
| **Quickchat AI Discord 2026 가이드** | https://quickchat.ai/post/create-ai-bot-for-discord | 2026 시점 Discord Developer Portal 의 정확한 토글 위치 / privileged intent 활성화 흐름. → init wizard 가이드 |

**비채택 reference (의도적 배제, 근거):**
- LibreChat Discord OAuth — *사용자 login* 용도지 봇 운영 X
- AnythingLLM Discord connector — manual config 만, 자동 연결 reference 가치 낮음
- n8n discord-trigger — 외부 webhook bridge 패턴, SoloSquad 는 long-running Node bot 직접 운영이라 추가 hop 불필요
- User Install (사용자 컨텍스트 설치) — Manage Channels 권한 부재로 SoloSquad 의 `#command` / `#workflow` 자동 생성과 충돌

---

## 3. 자동 연결 흐름 (3 진입 경로)

### 3.1 Default — OAuth Invite URL (사용자 own bot, 1 click 초대)

**전제:** 사용자가 Developer Portal 에서 Application + Bot 생성 + Token 발급 + MESSAGE_CONTENT intent 활성화 완료 (4 단계 — §3.3 manual 영역, init wizard 가 가이드).

**자동화 흐름:**

```
1. solosquad init 의 "Step 4 — Messenger" 분기에서:
   a) 사용자 Discord application client_id 입력 (Developer Portal URL 에서 복사)
   b) 사용자 Bot token 입력 (Developer Portal → Bot → Reset Token → 복사)
2. init 가 client_id + 권장 permissions bitfield (§4) 로 invite URL 합성:
   https://discord.com/oauth2/authorize?client_id=<id>&scope=bot+applications.commands&permissions=<bitfield>
3. URL 출력 + browser 자동 open (실패 시 clipboard 복사 fallback)
4. 사용자가 브라우저에서 "Authorize" click + 자기 길드 선택
5. Discord 가 봇을 길드에 추가 → SoloSquad bot 프로세스 (이미 running) 의 `guildCreate` 이벤트 발화
6. §5 onboarding 자동 시작
```

**원자성:** 사용자 click 1회 (Authorize). 그 외 9 단계는 모두 자동.

### 3.2 Alternative — BYO Bot manual (Developer Portal 자체 가이드)

`solosquad init --byo-bot-manual` (또는 init wizard 분기 명시 선택) 시 invite URL 자동 생성 skip. 사용자가 직접 OAuth URL 생성 → 초대. v1.0.4 까지의 기본 흐름과 동일. 자동화 1단계 줄어듦 — *통제 우선 사용자* 용.

### 3.3 자동화 가능 vs Discord 정책 manual

**Discord 정책상 manual (자동화 불가):**

| 단계 | 이유 |
|---|---|
| Discord Application + Bot 생성 | 사용자 계정 본인 인증 필요. API 우회 0. |
| Token 발급 / Reset | UI 만. Token Manager API 없음. |
| MESSAGE_CONTENT privileged intent 토글 | Developer Portal UI 만. Application API 에 없음. |
| OAuth "Authorize" click + guild 선택 | 사람 액션 필수. *어떤 봇이 어떤 길드에 access 할지* 의 명시적 동의. |

→ init wizard 가 각 단계의 *정확한 URL + 토글 위치 + 복사할 값* 안내 + clipboard helper (예: client_id prompt 직후 invite URL 을 clipboard 에 자동 복사). 사용자 manual 시간 ≤ 3분 목표.

**자동화 가능 (SoloSquad 책임):**

| 영역 | 메커니즘 |
|---|---|
| invite URL 합성 | `solosquad discord invite-url` (또는 init 내부 호출) |
| 봇 client login | discord.js `Client.login(token)` — v0.8.0 부터 동작 |
| guild join 감지 | `guildCreate` listener — v1.0.3 부터 동작 |
| 카테고리 + 채널 auto-create | `ensureChannels` — v0.8.0 부터 동작 (v1.0.4 가 robust 화) |
| guild-org binding 박제 | `syncGuildProductMapping` — v1.0.4 가 load-or-empty 로 완성 |
| Chief 첫 대화 trigger | §5 — `guildCreate` 후 systemChannel 에 onboarding embed + button |
| Slash command bulk register | `Routes.applicationGuildCommands` — guild scope 즉시 반영 |

### 3.4 SoloSquad 공식 봇 운영 mode (v1.x slot, 본 v1.2 미포함)

v1.x 에서 *SoloSquad 가 공식 봇 1개 운영* + 사용자가 자기 길드에 초대만 하는 mode 추가 검토:
- 사용자 manual 0 (토큰 발급 / intent 토글 / OAuth 모두 SoloSquad 가 사전 처리)
- Discord 100길드 도달 시 verification 필요 → SoloSquad 운영 부담
- 사용자 토큰이 SoloSquad 서버에 저장 → 보안/신뢰 부담

v1.2 는 BYO 단일 모드. 공식 봇 mode 는 사용자 base 가 충분히 형성된 후 별도 plan.

---

## 4. Bot Identity & Token (단일 Chief)

### 4.1 user.yaml 의 `bots.chief` 단일 entry

v0.8.0 의 single-bot user.yaml 스키마 그대로 사용 — `bots` 필드가 *항상 chief 1개*:

```yaml
# <org>/.solosquad/users/<handle>.yaml
handle: w1n
messenger: discord
messenger_user_id: "<discord-user-id>"  # 사용자 본인 Discord ID (v0.8.0)
bots:
  chief:
    bot_token_env: DISCORD_TOKEN          # v1.0.x 단일 토큰 그대로 — 별도 env var 신설 X
    bot_user_id: "1234567890"             # 봇이 처음 login 시 client.user.id 로 자동 채움
    application_client_id: "9876543210"   # v1.2 신규 — invite URL 합성에 필요
    skill_path: "<org>/agents/main/chief/SKILL.md"
    role: supervisor
    tier: leader
```

> **v0.8.0 user.yaml 호환:** 기존 `bot_user_id_legacy` 가 있으면 자동으로 `bots.chief.bot_user_id` 로 마이그레이션 (`1.1.0 → 1.2.0` migration step). breaking 0.

### 4.2 권장 permissions bitfield

`scope=bot applications.commands` + 다음 권한 (overkill 도 underkill 도 회피):

| Permission | bit | 필요 이유 |
|---|---|---|
| View Channels | 1024 | 메시지 수신 가능 채널 인지 |
| Send Messages | 2048 | Chief 응답 송신 |
| Send Messages in Threads | 274877906944 | thread-per-workflow (§6.2) |
| Create Public Threads | 34359738368 | thread 자동 시작 |
| Manage Threads | 17179869184 | auto-archive / rename |
| Read Message History | 65536 | referencedMessage chain (§7.3) |
| Embed Links | 16384 | 결과 보고 embed |
| Attach Files | 32768 | design doc / 이미지 첨부 |
| Use Application Commands | 2147483648 | `/chat` slash (§7.4) |
| Manage Channels | 16 | `#command-<handle>` / `#works-<handle>` 자동 생성 (§5) |

**합계 bitfield**: 합산 값은 publish 직전 calculator 로 재산정 후 invite-url 모듈 상수로 박제 (`DEFAULT_PERMISSIONS_BITFIELD`).

**제외 (Discord 심사 트리거 위험):** `Administrator`, `Manage Guild`, `Manage Roles`, `Kick/Ban`, `Mention Everyone`.

### 4.3 토큰 1개

- 단일 `DISCORD_TOKEN` env var — v1.0.x 와 동일 위치 (`<workspace>/.solosquad/.env`).
- multi-bot env var (`DISCORD_TOKEN_CHIEF` / `_PM` / `_ENG` / ...) 도입 보류 (v1.2 scope 외).
- 토큰 검증: `solosquad doctor --discord` 가 Discord REST `/users/@me` 호출로 token 유효성 1초 내 확인 + bot_user_id mismatch 시 actionable hint.

### 4.4 Single source of truth = Chief 봇 자체

본 v1.2 는 별도 `bots.json` registry 신설 *안 함*. v1.0.4 의 `<org>/discord/config.yaml` (load-or-empty 자동 생성) 이 *Chief 봇 ↔ guild ↔ org* 매핑 single source. v1.1.0 의 chief-runner 는 user.yaml.bots.chief 만 보면 됨.

---

## 5. guildCreate Onboarding (button-confirmed channel auto-provision)

### 5.1 흐름

```
[봇이 길드에 초대됨]
  ↓
Discord Gateway → solosquad bot process: guildCreate 이벤트
  ↓
[Chief onboarding-embed 생성]
  guild.systemChannel
    ?? guild.channels.cache.first(writable text channel by bot)
  로 송신
  ↓
[Embed 내용]
  Title: "안녕하세요, Chief 입니다 🫡"
  Description (3 줄):
    - 저는 [도메인] 전문가입니다. 사용자와의 대화에 집중합니다.
    - 다음 채널을 자동으로 만들까요? `#command-<handle>` (지시) + `#works-<handle>` (작업 결과)
    - 준비되면 `#command-<handle>` 에서 첫 목표를 알려주세요.
  Buttons:
    [✓ Auto-create channels] (custom_id: chief:onboard:auto)
    [⚙ Manual choose]        (custom_id: chief:onboard:manual)
  ↓
[사용자 button click]
  - "Auto-create" → ensureChannels(guild) 호출 (v1.0.4 로직 그대로) + 첫 인사 메시지 송신
  - "Manual choose" → channel mention prompt + 사용자가 #채널 선택 시 박제
  ↓
[채널 생성 완료]
  Chief 가 `#command-<handle>` 에 첫 메시지:
    "준비 완료! 첫 목표를 알려주세요.
     예시:
       • 이번 주 PMF 가설 검증 루틴 설계
       • 신규 기능 PRD 초안 만들기
       • [경쟁사 X] 변화 모니터링 routine 등록"
```

### 5.2 Idempotency

- `guildCreate` 가 중복 발화 (봇 재시작 / 길드 재초대) 해도 안전:
  - 채널 이미 존재 → 생성 skip (v1.0.4 `ensureChannels` 멱등)
  - onboarding-embed 이미 송신 → systemChannel 의 최근 N 메시지에 botUserId 가 보낸 동일 embed 가 있으면 재송신 skip
  - 첫 인사 메시지 → `#command-<handle>` 의 가장 오래된 봇 메시지 hash 로 dedupe

### 5.3 Button Interaction Handler

```typescript
// src/messenger/discord-onboarding.ts (신규)
client.on(Events.InteractionCreate, async (interaction) => {
  if (!interaction.isButton()) return;
  if (!interaction.customId.startsWith("chief:onboard:")) return;

  const action = interaction.customId.split(":")[2]; // "auto" | "manual"
  if (action === "auto") {
    await interaction.deferReply({ ephemeral: true });
    await ensureChannels(interaction.guild!);
    await interaction.editReply("✓ 채널 생성 완료. #command-<handle> 로 가세요.");
  } else if (action === "manual") {
    // 사용자가 채널 mention 으로 직접 지정하는 다단계 prompt
    await interaction.reply({ content: "지정할 채널을 mention 으로 알려주세요. 예: <#1234567890>", ephemeral: true });
  }
});
```

### 5.4 권한 부족 case

봇이 채널 생성 권한 없는 길드 (사용자가 OAuth 단계에서 `Manage Channels` 권한 disable) 에 초대된 경우:
- Chief embed 가 송신 X (systemChannel write 권한도 없을 수 있음)
- 봇 DM 으로 사용자에게 *"권한 부족. 봇 재초대하거나 `Manage Channels` 권한 부여"* + 새 invite URL 첨부

---

## 6. Channel + Thread Topology

### 6.1 v1.0.x 페어 유지

- `#command-<handle>` — 사용자 input 채널. Chief 만 listen. (v0.8.0)
- `#works-<handle>` — Chief 의 비-conversational output (routine 결과, broadcast). (v0.8.0)

### 6.2 v1.2 추가 — Thread per Workflow (gpt-discord-bot 패턴)

- Chief 가 사용자 메시지를 TRIAGE → workflow 로 분류한 순간:
  ```typescript
  const thread = await message.startThread({
    name: `wf-${YYYYMMDD}-${slug}`,
    autoArchiveDuration: 10080, // 7 days
    reason: `SoloSquad workflow ${workflowId}`,
  });
  ```
- 이후 모든 dispatch / synthesize / decide narration 은 thread 내부.
- `#command-<handle>` 채널 본문에는 *thread 링크 + 1줄 요약* 만 남음.
- 사용자가 thread 안에서 추가 대화 → Chief 가 같은 workflow context 유지 (referencedMessage chain, §7.3).

### 6.3 Thread ↔ Workflow 1:1 매핑

- `<org>/workflows/wf-YYYY-MM-DD-<slug>/discord-thread.txt` — thread URL + thread_id 박제
- chief-runner 가 워크플로우 reconcile 시 (`_status.yaml` 에서 stage 부활) 같은 thread 에 *"thread 재개됨"* 알림

### 6.4 Forum Channel 사용 안 함

v1.2 초안의 Forum Channel topology 폐기. 이유: solo founder 1인 환경에서 *모든 메시지 = thread* 강제는 메타데이터 overhead + 모바일 UX 저하. text channel + 동적 thread 가 더 가벼움.

---

## 7. Chief 대화 UX

### 7.1 사용자 → Chief → 응답 흐름

```
[사용자 메시지 in #command-<handle>]
  ↓
Discord adapter messageCreate → onCommand(text, product, ctx)
  ↓
chiefRunner.handleUserMessage({ userText, orgSlug, orgCwd, userId })
  ↓
Chief 6+1 stage (v1.1 §5.2):
  TRIAGE → DECOMPOSE → DISPATCH → AWAIT → SYNTHESIZE → DECIDE → RETROSPECT
  ↓
chiefRunner returns { text, costUsd, durationMs, ... }
  ↓
Discord adapter sends `text` to message.channel (또는 §6.2 thread)
```

이미 v1.1.0 에 구현된 wiring. v1.2 는 thread 라우팅 분기 + onboarding embed + button interaction 만 추가.

### 7.2 Typing indicator + 응답 streaming

- Chief 가 응답 생성 중: `message.channel.sendTyping()` (이미 v0.8.0 부터 동작)
- 응답 길이 > 2000 자 (Discord limit): chunk 분할 + 첫 chunk 에 thread 시작 권유

### 7.3 Message Context (referencedMessage chain — llmcord 패턴)

- 사용자가 봇 메시지에 reply (Discord 의 message.reference) 하면 Chief 는 referencedMessage + 그 references chain 을 컨텍스트에 inject
- 최대 chain 깊이 = 8 (워크플로우 thread 안에서는 thread 전체)
- chain 중 봇 자신 메시지와 사용자 메시지 모두 chronological order 로 inject
- LRU message cache (1000 entries / 길드) 로 Discord REST `GET /channels/{id}/messages/{id}` 호출 절약

### 7.4 `/chat` slash command (low-intent backup)

MESSAGE_CONTENT privileged intent 가 거부된 경우 (Discord verification 우려, 또는 100길드 초과) 대안:

```
/chat <message>
```

- Discord 가 slash command interaction 으로 메시지 본문 전달 → intent 무관
- Chief 가 slash 응답을 thread 로 자동 분기 (gpt-discord-bot 패턴)
- 본 v1.2.0 에는 register 만, default 라우팅은 여전히 message + mention. 100길드 도달 시 slash-first 로 전환.

---

## 8. 과제 진행 / 보고 (Chief 6+1 stage → Discord projection)

### 8.1 Stage event → Discord 메시지 변환

`<org>/memory/chief-stage-events.jsonl` (v1.1 §5.2) 의 각 event 가 Discord thread 내 메시지로 자동 projection. 변환 표:

| Chief stage | Discord narration (Korean) |
|---|---|
| TRIAGE | (생략 — 너무 짧음. Chief 가 사용자에게 직접 응답하는 turn 의 일부) |
| DECOMPOSE | "🗂 작업 분해 중... PM 호출 결정." |
| DISPATCH | "📤 dispatch: pm, engineer (병렬 2)" |
| AWAIT (open_questions[]) | "❓ 추가 정보 필요:\n  • [Q1] 타깃 사용자 어느 segment?\n  • [Q2] 배포 timing 우선?" (사용자 응답 대기) |
| SYNTHESIZE | (생략 — 결과 메시지 자체로 표현) |
| DECIDE | 최종 응답 본문 = Chief 의 사용자 reply |
| RETROSPECT | 별도 메시지 X. retrospective skill 결과는 weekly-retro 에서 broadcast |

### 8.2 Sub-agent 활동 narration

PM / Engineer / Designer / Marketer 가 spawn 되었을 때 사용자가 *지금 누가 뭘 하고 있는지* 확인 가능:

```
[사용자: 새 랜딩 페이지 만들자]
[Chief: 작업 분해 중]
[Chief: dispatch — pm + designer]
  ↳ [pm] discovery-synthesis 실행 (archive 8건 분석)
  ↳ [pm] problem-definition P3 MECE 진행
  ↳ [designer] ux-designer wireframe 초안
[Chief: 통합 완료. design doc 작성됨 → <thread link>]
```

각 sub-step 메시지는 `chief-stage-events.jsonl` 의 `dispatched` / `skills_used` 필드를 message text 로 format. 빈도 너무 잦으면 (debounce 5초) 묶음.

### 8.3 open_questions[] handling

PM 이 `<org>/memory/open-questions/<task-id>.json` 에 blocking 질문을 남기면:
- Chief 가 해당 task 의 Discord thread 에 batch 메시지로 출력 (v1.1 §6.3 — 사용자에게 한 번에)
- 사용자가 같은 thread 에서 reply → Chief 가 referencedMessage chain 으로 answer 매칭 → PM 재spawn

### 8.4 status / progress 자가 보고

사용자가 *"지금 뭐 하고 있어?"* 식 메시지 → Chief 가 `chief-stage-events.jsonl` 최근 30분 읽어서:

```
현재 진행:
  • turn-<id> AWAIT (PM 의 open_questions 2건 사용자 응답 대기)
  • turn-<other> DISPATCH 직후 (engineer 2 spawn 진행 중, 평균 3분 예상)

최근 완료:
  • 12분 전 RETROSPECT — 어제 캠페인 회고 (skill-refinement 1건 제안됨)
```

이 self-report 는 `solosquad status` CLI 의 Chief-version (skill `triage` 의 일부로 구현).

---

## 9. Safety Net (echo guard, token budget, rate limit)

### 9.1 Echo guard (단일 봇 환경)

5-bot 환경의 봇간 mention loop 가 사라졌으므로 echo guard 도 단순화:
- 봇은 `message.author.id === client.user.id` 메시지 무조건 skip (v0.8.0 부터 동작)
- 사용자가 같은 메시지를 1초 내 ≥3회 반복 송신 시 Chief 가 *"같은 요청을 받았어요. 한 번만 처리할게요."* + 첫 메시지만 처리

### 9.2 Token budget per thread

- workflow thread 의 누적 응답 token > `workspace.yaml.discord.thread_token_budget` (default: 80,000 ≈ 한 Claude 컨텍스트) 초과 시 Chief 가 *"thread 컨텍스트가 가득 찼어요. 새 thread 로 이어갈까요?"* prompt
- 사용자 동의 시 새 thread 시작 + 직전 thread 의 design doc / decision 요약 link

### 9.3 Discord rate limit

- discord.js v15 가 자동 처리 (`rest.setRetryLimit`, 429 백오프)
- 추가 안전망: Chief 의 sub-agent narration (§8.2) 메시지를 5초 debounce + 동일 thread 내 연속 메시지 batch ≤5

### 9.4 작성 권한 회수 시나리오

봇이 길드에서 채널 권한 회수당함 → discord.js 가 `DiscordAPIError: Missing Permissions` throw → Chief 가 캐치해서 DM 으로 사용자에게 알림 + invite URL 재발급

---

## 10. 5-hop Binding Diagnostic (v1.0.4 P + 단일봇 정합)

`solosquad doctor --discord` 가 다음 5 hop 모두 통과해야 connect ready:

| Hop | 검증 | 실패 시 hint |
|---|---|---|
| 1 | DISCORD_TOKEN env 존재 + format (`<id>.<timestamp>.<hmac>`) | `Set DISCORD_TOKEN in <workspace>/.solosquad/.env.` |
| 2 | Token 으로 Discord REST `/users/@me` 호출 → 봇 user ID 받음 | `Token 무효. Developer Portal → Bot → Reset Token 후 재입력` |
| 3 | bot_user_id 가 user.yaml.bots.chief.bot_user_id 와 일치 | `Bot ID 불일치. Reset Token 후 doctor 재실행하면 자동 갱신됨` |
| 4 | 봇이 ≥1 guild 에 속해 있음 (Gateway READY 후 guilds.cache) | `OAuth Invite URL 사용해 길드에 초대하세요: ${invite_url}` |
| 5 | `<org>/discord/config.yaml.guild_id` 가 길드 중 하나와 일치 + `loadProducts()` 에 해당 org 포함 + `command-<handle>` 채널 존재 | `solosquad init` 또는 `solosquad add-org` 재실행. config.yaml load-or-empty 자동 채움. |

각 hop 실패는 attributable + actionable. silent fail 0.

---

## 11. v1.2.x — Slack 분리

본 v1.2.0 은 Discord only. Slack 동등 기능 (slash command, channel auto-create, thread-per-workflow, app_mention intent) 은 v1.2.1+ sub-version 으로:
- 같은 ChiefMessenger 인터페이스를 Slack adapter 가 구현 (v0.8.0 adapter pattern 그대로)
- Slack 의 *Add to Slack* button = Discord OAuth Invite URL 등가물
- 채널 토폴로지: standard channel + thread (Slack Canvas 는 별도 slot)
- Slack 의 `app_mention` event = Discord MESSAGE_CONTENT intent 등가물 — Slack 은 default 활성, intent 토글 없음

Slack v1.2.x 는 본 plan 의 §3 / §5 / §7 / §8 패턴을 그대로 Slack API 위에서 재구현. 새 plan doc 신설 (`v1.2.1-slack-messenger-connection.md`) 권장.

---

## 12. 구현 체크리스트 (작업 11건)

### Phase A — 자동 연결 코어 (v1.2.0 필수)

| # | 작업 | 우선순위 | 의존 |
|---|---|---|---|
| 1 | `solosquad discord invite-url` CLI 신설 — application_client_id + permissions bitfield 합성 + browser open + clipboard fallback | high | — |
| 2 | `user.yaml.bots.chief.application_client_id` 필드 신설 + v1.1.0→1.2.0 migration (`bot_user_id_legacy` → `bots.chief.bot_user_id` + `application_client_id` prompt) | high | #1 |
| 3 | init wizard Step 4 — Discord 분기 보강. Developer Portal 가이드 + token prompt + invite URL 합성 + browser open | high | #1, #2 |
| 4 | `guildCreate` 핸들러 — onboarding embed + 2 button (Auto-create / Manual) | high | — |
| 5 | Button interaction handler — `chief:onboard:auto` 시 ensureChannels + 첫 인사 / `chief:onboard:manual` 시 channel mention prompt | high | #4 |
| 6 | `solosquad doctor --discord` 5-hop diagnostic | high | #2 |

### Phase B — Chief 대화 UX (v1.2.0)

| # | 작업 | 우선순위 | 의존 |
|---|---|---|---|
| 7 | message → thread 자동 분기 — Chief 가 TRIAGE 결과 = workflow 면 `message.startThread()` + `<org>/workflows/.../discord-thread.txt` 박제 | high | — |
| 8 | referencedMessage chain — Discord reply 시 chain 깊이 8까지 fetch + LRU cache (1000/guild) | medium | — |
| 9 | chief-stage-events → Discord narration projection — DISPATCH / AWAIT 의 message format + 5초 debounce | medium | — |
| 10 | `/chat` slash command register (intent fallback) — guild scope, immediate | low | — |

### Phase C — Safety / Robustness (v1.2.0)

| # | 작업 | 우선순위 | 의존 |
|---|---|---|---|
| 11 | Thread token budget warning — `workspace.yaml.discord.thread_token_budget` (default 80k) + Chief auto-prompt 새 thread | medium | #7 |

### 의도적 보류 (v1.2.x 후속)

- 5 봇 multi-bot 모델 — v1.2 본 plan 범위 외 (§0 폐기)
- Forum Channel topology — v1.x slot
- 9-hop diagnostic — 5-hop 으로 충분 (단일봇)
- Slack adapter — v1.2.1+
- SoloSquad 공식 봇 운영 mode — v1.x

---

## 13. Backward Compatibility

### 13.1 v1.1.0 사용자

- `solosquad migrate --apply` (1.1.0 → 1.2.0) 가 자동:
  - `user.yaml.bots.chief.application_client_id` 가 비어있으면 doctor 가 fix-up 안내 (init 재실행 또는 manual edit)
  - `bot_user_id_legacy` 가 있으면 `bots.chief.bot_user_id` 로 복사 (idempotent)
- 기존 channel / token / guild binding / config.yaml 무손상
- 기존 chief-runner / chief-stage-events.jsonl / open-questions/ / ledger/ 모두 그대로 동작

### 13.2 v1.0.x 직접 migrate (chained 1.0.x → 1.1.0 → 1.2.0)

- migration chain: 1.0.2 → 1.0.3 → 1.0.4 → 1.1.0 → 1.2.0
- v1.0.x → v1.1.0 step (`1.0.2-to-1.1.0.ts` with `from: 1.0.4.x`) 가 Chief / team / open-questions / ledger 시드 ✓
- v1.1.0 → v1.2.0 step 이 application_client_id 처리

### 13.3 schema 변경

- `user.yaml.bots.chief.application_client_id` (신규, optional — doctor 가 missing 시 prompt)
- `workspace.yaml.messenger.discord.install_mode` (`oauth_invite` default | `byo_manual`)
- `workspace.yaml.discord.thread_token_budget` (default 80000)
- breaking 0 (모두 신규 optional)

### 13.4 CLI surface

- 신규: `solosquad discord invite-url`, `solosquad doctor --discord`
- 기존 변경: `solosquad init` Step 4 — Discord 분기 내부 강화 (커맨드 signature 변경 0)
- v1.0.0 freeze 42 명령어 모두 유지

---

## 14. Out of Scope (의도 명시)

본 v1.2 가 다루지 않는 영역:

- **5 봇 multi-bot (chief + pm + engineer + designer + marketer)** — §0 폐기. v1.x slot
- **Forum Channel topology** — text channel + dynamic thread 가 solo founder 에 충분 (§6.4)
- **Bot Identity Registry `bots.json` 5-entry** — single Chief 만, user.yaml.bots.chief 로 충분
- **9-hop diagnostic** — 5-hop reduce
- **SoloSquad 공식 봇 운영 (사용자 토큰 발급 0)** — 사용자 base 확보 후 별도 plan
- **Slack adapter** — v1.2.1+ sub-version
- **모바일 native app / 웹 대시보드** — 별도 repo (`solopreneur-dashboard`)
- **다중 메신저 동시 (Discord + Slack 같은 org)** — 1 org = 1 messenger primary (v0.8 §3.6 broadcast 는 cross-feed)

---

## 15. 변경 이력 / 레퍼런스

### 15.1 변경 이력

- **2026-05-27 (현재 — v1.2 전면 재작성)**: 사용자 directive "1 Chief / org / Discord 자동 연결" 반영. 5-bot multi-bot 모델 폐기, Bot Identity Registry 5-entry 폐기, Forum Channel topology 폐기, 9-hop → 5-hop. 새 §3 (OAuth Invite URL 자동 발급) + §5 (guildCreate onboarding button) + §6.2 (thread-per-workflow) + §8 (chief-stage-events → Discord narration projection) 신설. 외부 reference 6건 (llmcord / gpt-discord-bot / Composio / Discord OAuth2 docs / discord.js v15 / Quickchat 2026 가이드) 새 조사 보고서 통합.
- **2026-05-27 (이전 — v1.1.0 출시 반영)**: bot_name → tier/team/used_by, "5 코어 봇" → "5 main bot", PM session → Chief, assets/orchestrator → <org>/agents/main/chief, agents/specialists/ 평탄 + composition.yaml.
- **2026-05-24**: 최초 작성. mega-plan 분리 (v1.1 에이전트 팀 + v1.2 메신저). 5 bot multi-bot 가정.

### 15.2 내부 의존 (참조 docs)

- `docs/prd/v1.1-multi-agent-team-architecture.md` — **hard dependency, 출시 완료** (Chief Sub-System §5 + PM Sub-System §6 + chief-stage-events §5.2 + open_questions[] §6.3)
- `docs/prd/v1.0-official-launch.md` — api-stability 발효
- `docs/prd/v1.0.1-discord-ready-deprecation.md` §2.4 — `@<slug>` mention parser
- `docs/prd/v1.0.2-discord-author-guard-decoupling.md` — Discord ACL = canonical permission boundary
- `docs/prd/v1.0.3-discord-triple-bug-fix.md` — guild-org binding, category lookup
- `docs/prd/v1.0.4-messenger-config-auto-create.md` — config.yaml load-or-empty + 5-hop diagnostic base

### 15.3 외부 reference (2026-05-27 조사 보고서)

| Source | 채택 영역 |
|---|---|
| llmcord (jakobdylanc) | §7.3 referencedMessage chain + LRU cache |
| openai/gpt-discord-bot | §6.2 thread-per-workflow, §7.4 /chat slash |
| Composio Discord toolkit | §8 agent → Discord API 추상화 패턴 |
| Discord OAuth2 docs | §3.1 invite URL + permissions bitfield |
| discord.js v15 가이드 | §3.3 intent 의존도 / §5.3 button interaction |
| Quickchat AI 2026 가이드 | init wizard Developer Portal 정확한 토글 위치 |

(비채택 reference 와 이유 — §2)

---

## Appendix A · 신규 파일 / 디렉토리 / 환경변수

### 신규 파일

- `src/messenger/discord-onboarding.ts` — guildCreate listener + button interaction handler + onboarding embed 생성기
- `src/messenger/discord-invite-url.ts` — invite URL 합성 + browser open + clipboard fallback
- `src/messenger/discord-thread-store.ts` — workflow ↔ thread URL 매핑 (`<org>/workflows/.../discord-thread.txt`)
- `src/messenger/discord-narration.ts` — chief-stage-events → Discord 메시지 formatter (debounce 5초)
- `src/messenger/discord-message-cache.ts` — referencedMessage LRU cache (1000/guild)
- `src/cli/discord.ts` — `solosquad discord invite-url` CLI 그룹
- `src/cli/doctor.ts` (확장) — `--discord` 5-hop diagnostic
- `src/migrations/scripts/1.1.0-to-1.2.0.ts` — application_client_id prompt (idempotent), bot_user_id_legacy 흡수
- `test/discord-invite-url.test.ts` — URL 합성 + permissions bitfield 검증
- `test/discord-onboarding.test.ts` — guildCreate handler + button interaction (mocked guild)
- `test/discord-narration.test.ts` — stage event → message format + debounce
- `test/discord-doctor.test.ts` — 5-hop diagnostic case

### 신규 디렉토리

- 없음 (기존 `src/messenger/`, `src/cli/`, `src/migrations/scripts/` 에 모두 흡수)

### 신규 환경변수

- 없음. 기존 `DISCORD_TOKEN` 그대로. (multi-bot 환경변수 `DISCORD_TOKEN_*` 도입 보류)

### 신규 schema 필드

- `user.yaml.bots.chief.application_client_id` (string, optional)
- `workspace.yaml.messenger.discord.install_mode` (enum: oauth_invite | byo_manual, default: oauth_invite)
- `workspace.yaml.discord.thread_token_budget` (number, default: 80000)

---

## Appendix B · v1.2 초안 (5-bot / Forum / 9-hop) 박제 — 의도적 폐기

본 plan 의 2026-05-24 ~ 2026-05-27 (1차 v1.1.0 반영) 초안은 *5 봇 직원화 (chief/pm/engineer/designer/marketer 각자 Discord identity)* + *Forum Channel 강제* + *9-hop diagnostic* 으로 작성됐었음. 2026-05-27 사용자 directive 로 **scope 축소**:

| 폐기 항목 | 폐기 사유 |
|---|---|
| 5 봇 multi-bot Discord identity | 사용자 인지 부담 (5 토큰 발급) + Discord 100길드 한도 5배 + 회귀 면적 5배 |
| `bots.json` 5-entry Bot Identity Registry | 단일 Chief 만 필요 — user.yaml.bots.chief 로 충분 |
| Forum Channel topology 강제 | solo founder 1인 환경에서 모든 메시지=thread 강제는 과함 — text + 동적 thread 가 더 가벼움 |
| 9-hop diagnostic | 5-hop 로 충분 (단일 봇) |
| Webhook persona overlay | Hermes V2 §3.3 약화 + 사용자 가치 불명확 |

이 폐기 영역들은 *영구 out-of-scope* 가 아니라 *v1.2 출시 시점의 trade-off*. v1.x 사용자 base 충분히 확보 후 일부 재도입 검토.

**EOF**
