# v1.3.8 — 문서 관리 체계 + `docs` 스킬 (도입)

> 본 PRD 는 솔로스쿼드 **내부 dev PRD 양식**(v1.3.7 과 동일: 배경 → 핵심 개념 →
> 스코프 → 손댈 파일 → 마이그레이션 영향 → 비범위 → 검증)을 따른다. founder 제품
> 기획 워크플로의 누적 컨테이너 양식(`prd` 스킬의 8-section / new-build·improvement
> 2양식)과는 **별개** — 이 둘의 구별이 §2·§3.1 의 설계 대상이기도 하다.
>
> 파일명은 새 규칙 `v1.3.8_docs-management.md`(`<version>_<name>`, 토큰 내부 `-`).
> 본 PRD 는 자신이 신설하는 `prd` 작성 5규칙(R1–R5, §3.7)을 **스스로 적용**(dogfooding).

## 0. 소속 버전 목표 (R1 — 상위 목표 상속)

`v1.3.8` → X=1 · Y=3 · **Z=8 (patch)**. patch 는 상위 목표를 **인용만** 하고 그 안에서
맡는 조각만 정의한다(재정의 금지).

| 자리 | 목표 (umbrella) | 출처 |
|---|---|---|
| **1.x.x** (major) | 워크플로우/goal/cron 고도화 + **암묵지 → primitive 자동화 강화** — 24/7 자율 팀이 자기 산출물을 표준대로 생산 | roadmap §2.3 |
| **1.3.x** (minor) | *primitive·문서 작성체계 내재화* — squad 가 primitive(skill·agent·workflow·goal·cron·docs)를 표준대로 **생산·관리** (1.3.0 messenger UX → 1.3.2 매니저+validate → 1.3.5 planning → 1.3.6 skill·agent authoring → 1.3.7 workflow·goal·cron authoring → 1.3.8 docs) | roadmap §2.3 (2026-06-25 박제) |
| **1.3.8** (this) | 위 1.3.x 의 **문서(docs) 조각** — 분류·버전결합·게이트·INDEX 의 단일 권위(`docs` 스킬) + PRD 작성 shape 확정 | 본 PRD |

> 경계: 1.3.8 은 *문서 관리 체계*만. 일정·메모(별도 미래 slot 으로 분리 — roadmap §2.1)·
> goal-runner 고도화는 별도 버전 — 본 PRD 가 끌어오지 않는다(§3.8).

## 1. 배경 / 문제

- 솔로스쿼드가 founder 에게 주는 *가시적* 가치 = 코딩 에이전트 플랜 모드를 고도화한
  **잘 쓴 PRD**. 기획 워크플로의 결과를 담고, 버전으로 관리되며, 아이데이션·리포트와
  구별되어 개발이 그 PRD 에서 출발하는 스펙.
- 그런데 **문서 관리 권위가 없다.** `prd` 스킬은 *한 PRD 를 어떻게 쓰나*(8-section)만
  갖췄고, *어디에·어떤 버전으로·언제 최신화·외부냐 내부냐*의 큐레이션 층이 없다.
- **솔로스쿼드 자기 docs 실태:** `docs/prd/`(52)에 버전 PRD + `architecture.md` +
  `product-roadmap.md` 혼재 · `docs/ideation/`(19)에 리포트 혼재 · `docs/reports/`
  부재(기존 `prd` 스킬은 이미 그 경로를 전제) · 명명 불일치(`v0.1-…` 2자리,
  `V0.3-INTEGRATION-TEST-PLAN` 대문자).
- **외부/내부 경계가 암묵적:** `package.json.files`(=`docs/` 제외, `manual/`·
  `CHANGELOG`·README 포함)로 사실상 강제되지만, 어떤 스킬도 이를 명문화·검사하지 않음.
- **PRD shape 혼동(본 PRD 가 자초한 실증):** 내부 dev PRD 와 founder 제품용 PRD 가
  같은 "PRD" 라는 이름을 공유해 혼선. 관리 체계가 이를 분리해야 한다.

### 1.1 As-Is / To-Be

| 관점 | As-Is (현재) | To-Be (v1.3.8 이후) |
|---|---|---|
| **관리 권위** | 없음 — `prd` 스킬이 *한 PRD 작성*만, 큐레이션 층 부재 | `docs` 스킬이 분류·버전·게이트·INDEX 의 단일 권위 (`prd` 는 per-PRD writer 로 역할 분리) |
| **외부/내부 구분** | 암묵적 — `package.json.files` 로만 사실상 강제, 명문화·검사 없음 | 1차 축으로 명시 + 게이트 불변식이 `docs/` 누출 차단 |
| **prd 폴더** | 버전 PRD + `architecture.md` + `product-roadmap.md` 혼재(52) | 버전 PRD 만. architecture·roadmap 은 `docs/` 직속 living 문서로 승격 |
| **report** | 디렉토리 부재 — `ideation/`·`trend-record/` 에 산재 | `docs/reports/` 신설, 리포트 집결, `evidence_ref` 대상 명확 |
| **ideation/report/prd** | 이름·위치로만 막연히 구분, 혼재 발생 | 발산/근거/수렴 으로 역할·생애주기·버전결합 분리 |
| **명명** | 불일치(`v0.1-…` 2자리, `V0.3-…` 대문자, 혼합 구분자) | `<version>_<name>_<date>`(토큰 `_`, 내부 `-`, 날짜 YYMMDD). 발행분 fix-forward |
| **PRD↔버전** | 느슨 — 파일명 prefix 관습뿐 | PRD 1:1 배포 버전. `docs` 가 생성 시 검증, 게이트가 tag·package.json 일치 확인 |
| **배포 전 최신화** | 4종 게이트(roadmap·architecture·manual ko/en) | 6종(+CHANGELOG·README) + PRD 존재 + `docs/` 누출 불변식 |
| **PRD shape** | 내부 dev / founder 제품 PRD 혼동(이름 충돌) | `docs` 스킬이 맥락별 shape 를 명문화·분기 |
| **discovery** | 카테고리 INDEX 없음(trend-record 만 보유) | prd·reports·ideation 에 `INDEX.md` |

위 표 = **개념적 변화**(역할·의미축). 아래 = **형태(경로) 변화**(R4 의 둘째 층):

```
As-Is                                  To-Be (v1.3.8)
docs/                                  docs/
├─ prd/                                ├─ prd/                 ← 버전 PRD 만
│  ├─ v1.3.8_*.md   (버전 PRD)         │  ├─ v1.3.8_*.md
│  ├─ architecture.md    ⎫ 혼재        │  └─ INDEX.md          ⊕신규
│  └─ product-roadmap.md ⎭             ├─ reports/             ⊕신규 디렉토리
├─ ideation/                           │  ├─ <name>_<YYMMDD>.md ← 산재 리포트 이동
│  ├─ <name>.md                        │  └─ INDEX.md          ⊕신규
│  └─ AI_..._Report.md  ← 리포트 산재   ├─ ideation/
└─ (reports/ 부재)                      │  └─ INDEX.md          ⊕신규
                                       ├─ architecture.md      ↑승격 (living)
                                       └─ roadmap.md           ↑승격 (living)
```

⊕신규 · ↑승격(경로 이동) · 발행분 PRD 명명은 immutable(fix-forward).

### 1.2 Objective (정합 목표)

- **정합 비전:** roadmap §"PRD = founder 에게 주는 가시 가치" — 잘 쓴 PRD 가 코딩
  에이전트 플랜 모드를 대체·고도화한다. 본 PRD 는 그 PRD *를 담는 그릇*(분류·버전·
  게이트)을 세워 founder 가 산출물을 신뢰·재사용하게 만드는 게 목표.
- **이 릴리즈가 움직이는 것:** ① 문서 누출/혼재로 인한 founder 혼선 0 화 ② 배포 시
  외부 문서 최신화 누락 0 화(게이트가 강제) ③ PRD discovery 가능(INDEX).
- **권고 / 반증:** "단일 권위 `docs` 스킬 + 외부/내부 1차 축이 혼재를 푼다" 고 판단.
  반증 조건 — 재정비 후에도 비-PRD 가 `docs/prd/` 에 재유입되거나(분류 미준수) 게이트
  우회 배포가 발생하면, 스킬-기반 큐레이션 가설이 틀린 것 → 정책+CI 강제로 선회(§6 D1).

### 1.3 지시 요구사항 (R3 — 전사 + 충족 추적)

founder/Chief 지시에서 추출한 요구사항. 각 항목이 스코프의 어디서 충족되는지 매핑(누락 방지):

| # | 요구사항 (지시 전사) | 충족 위치 | 상태 |
|---|---|---|---|
| Q1 | 기획 산출(PRD/ideation/report)을 외부·내부 축으로 분류·관리 | §3.2 분류표 + §2-1 | ✅ |
| Q2 | PRD ↔ 배포 버전 1:1 결합 | §3.4 + §2-2 | ✅ (예외 규칙 = OQ#1) |
| Q3 | 단일 권위 `docs` 스킬 신설, `prd` 와 역할 분리 | §3.1 + §6 D1 | ✅ |
| Q4 | architecture/roadmap 승격 + reports/ 신설 | §3.6 + §1.1 트리 | ✅ |
| Q5 | 배포 게이트 4 → 6종 | §3.5 | ✅ |
| Q6 | (본 대화) PRD 작성 5규칙을 `prd` 스킬·기획 워크플로에 반영 | §3.7 | ✅ |
| Q7 | (본 대화) 작업 체크리스트를 연관 에이전트가 워크플로에서 검토·개선 | §3.7 R5 + requirements-analysis §리뷰 | ✅ |

**방법 결정이 필요한 지점** (임의 선택 금지 → §6 approaches / §7 OQ 로 승격):
- `[결정 필요: docs 권위 = 신규 스킬 vs prd 확장 vs 정책+CI]` → §6 D1 (채택 A, 반증조건 명시)
- `[결정 필요: PRD↔버전 1:1 의 핫픽스·복수 ship 예외]` → §7 OQ#1 (blocking)
- `[결정 필요: founder org 강제 시드 vs 옵트인]` → §7 OQ#2 (blocking)

## 2. 핵심 개념

1. **문서 1차 축 = 외부(사용자 대면) vs 내부(기획·개발 전용).** 경계는
   `package.json.files` 로 강제(load-bearing). 내부 문서는 npm 패키지로 새지 않고,
   외부 문서는 배포 게이트로 최신화된다.
2. **PRD ↔ 배포 버전 1:1.** PRD 가 "이 버전에 무엇이 들어가나"의 single source.
   `package.json.version = vN.N.N → git tag → publish`. PRD 자체는 외부로 안 나가고
   manual·CHANGELOG 가 외부 노출을 대행.
3. **ideation ≠ report ≠ prd.** ideation="왜/만약"(발산·결정 전·폐기 안 함) ·
   report="무엇이 사실인가"(근거 스냅샷·PRD 가 `evidence_ref` 인용) · prd="무엇을 왜
   어떻게"(수렴·개발계획+요구사항·버전 결합·ship 후 frozen).
4. **PRD shape 는 맥락별로 다르다.** ⑴ 솔로스쿼드 내부 dev PRD(본 양식) ⑵ founder
   제품 기획 PRD(`prd` 스킬 8-section + new-build/improvement 누적 2양식). `docs`
   스킬이 어느 맥락에 어느 shape 인지 명문화한다.
5. **명명 규칙: `<version>_<name>_<date>`** — 세 토큰을 `_` 로, 토큰 내부 띄어쓰기는
   `-` 로. 존재하는 토큰만 사용. 날짜는 `YYMMDD` 압축형(하이픈 혼동 방지).

## 3. 스코프

### 3.1 `docs` 스킬 신설 (단일 권위)

- `skills/docs/SKILL.md`(`name: docs`, `team: _skill`, `category: planning`,
  `used_by: ["product-manager", "chief"]`). cron-manager 류 대화형 큐레이션 매니저.
- 보유: 문서 분류 체계(§3.2) · 명명 규칙(§3.3) · PRD↔버전 결합(§3.4) · 배포 게이트
  절차(§3.5) · 카테고리별 `INDEX.md` 유지 · **PRD shape 분기 규칙**(§2-4).
- 결정적 동작은 헬퍼/스크립트 위임(파일 직접 조작은 검증 게이트 경유). 분류·배치·
  최신화 판단은 대화. `skills/skill-core/primitive-core.md` 작성 표준 준수.
- 기존 `skills/prd/SKILL.md` 는 **per-PRD writer 로 유지**(역할 분리): `docs`=어디에·
  어떤 버전으로·언제 최신화, `prd`=한 PRD 를 어떻게 쓰나.

### 3.2 문서 분류 체계 — 외부/내부

| 축 | 타입 | 위치 | npm | 청자 | 기획모드 |
|---|---|---|---|---|---|
| 🌐 외부 | readme | `README.md` / `README.kr.md` | ✅(자동) | 잠재 사용자·기여자 | 진입점 |
| | changelog | `CHANGELOG.md` | ✅ files | 사용자 | 릴리즈 이력 |
| | manual | `manual/master-guide_{ko,en}.html`, `manual/*.md` | ✅ files | **제품 사용자** | 사용 안내 |
| 🔒 내부 | prd | `docs/prd/v<N.N.N>_<name>.md` | ✗ | 기획·개발(본인) | 수렴·스펙 |
| | ideation | `docs/ideation/<name>_<YYMMDD>.md` | ✗ | 기획 | 발산 |
| | reports | `docs/reports/<name>_<YYMMDD>.md` ⚠️신설 | ✗ | 기획(근거) | 근거 |
| | poc | `docs/poc/<name>.{mjs,md}` | ✗ | 개발 | 실증(코드) |
| | policy | `docs/policy/<topic>.md` | ✗ | 개발 | 불변 계약 |
| | architecture | `docs/architecture.md` ⚠️승격 | ✗ | 개발 | 구조 단일진실(living) |
| | roadmap | `docs/roadmap.md` ⚠️승격 | ✗* | 기획(본인) | 비전(living) |

\* roadmap 외부 공개 제품이면 `package.json.files` 옵트인.

> 설계 근거(approaches 비교·가정 분류·open questions)는 §6–§7 에 분리 수록 —
> 본 §3 은 *무엇을* 의 스코프, §6 은 *왜 이 안인가*의 결정 근거.

### 3.3 명명 규칙 (확정)

`<version>_<name>_<date>` — 타입별 존재 토큰만:

| 타입 | 토큰 | 예 |
|---|---|---|
| prd | `version_name` | `v1.3.8_docs-management.md` |
| ideation | `name_date` | `docs-management_260624.md` |
| reports | `name_date` | `market-research_260624.md` |

- 버전 항상 3자리 `vN.N.N`. 발행분 비표준(`v0.1-…`·`V0.3-…`)은 immutable —
  **fix-forward**(신규만 규칙 준수, 과거 리네임 안 함).

### 3.4 PRD ↔ 배포 버전 결합

PRD 1개 = 배포 버전 1개. `docs` 스킬이 PRD 생성 시 버전 토큰을 검증하고,
`package.json.version`·git tag 와의 1:1 을 게이트(§3.5)에서 확인.

### 3.5 배포 전 문서 최신화 게이트 (4종 → 6종)

`scripts/check-docs-freshness.ts` 확장:
```
1. 해당 vN.N.N PRD frozen 존재
2~6. roadmap·architecture·manual(ko·en)·CHANGELOG·README 가 vN.N.N 멘션
7. 불변식: package.json.files 에 docs/ 미포함(내부 누출 방지)
```
`prepublishOnly` 에 wired 유지. 메모리의 3-docs/4-docs 게이트 규칙과 정렬.

### 3.6 솔로스쿼드 docs 재정비 (dogfooding)

- `docs/reports/` 신설 + 산재 리포트 이동(`ideation/AI_Agent_Harness_Report.md` 등).
- `docs/prd/architecture.md` → `docs/architecture.md`, `docs/prd/product-roadmap.md`
  → `docs/roadmap.md` 승격.
- prd·reports·ideation 에 `INDEX.md` 추가.

### 3.7 PRD 작성 shape 보강 — `prd` 스킬 + 기획 워크플로 (Q6·Q7)

`docs` 가 *어디에·어떤 버전으로* 라면, 한 PRD *를 어떻게 쓰나*의 shape 도 함께 확정한다
(맥락 상실·오버스펙 방지). `prd` 스킬에 5규칙 명문화 + 기획 워크플로 리뷰 게이트 강화:

- **R1 버전 계층 인지** — `vX.Y.Z` 자리수별 정의 범위(X=umbrella, Z=상위 인용). PRD 상단 소속목표 블록.
- **R2 §스코프 = 포함 + 이번-버전 비포함** — 경계를 스코프에서 직접(영구 §비범위와 구분).
- **R3 지시 요구사항 전사 + 결정 표시** — 요구사항→충족 추적, `[결정 필요]` 인라인.
- **R4 As-Is/To-Be = 개념 + 형태 이중** — 표/다이어그램, 의미축 + 경로/구조 변화 둘 다.
- **R5 작업 체크리스트 = 워크플로 리뷰 대상** — `requirements-analysis` §리뷰에서 담당
  스페셜리스트가 실행가능성·**오버스펙**·누락 검토해 가감·확정(리뷰 전 non-binding).
- 손댈: `skills/prd/SKILL.md`(5규칙 + HARD GATE) · `requirements-analysis/workflow.yaml`(리뷰 가드).

### 3.8 이번 버전에서 다루지 않음 (R2 — 보류, 영구 비범위 §8 과 구분)

스코프 경계를 명시해 오버스펙을 차단(후속 버전에서 다룰 수 있음):

| 보류 항목 | 이유 | 후속 |
|---|---|---|
| PRD↔버전 1:1 의 핫픽스·복수 ship 예외 규칙 | OQ#1 미해소, 1.3.8 핵심 아님 | 후속 patch |
| founder org 자동 docs 마이그레이션의 강제/옵트인 정책 | OQ#2 미해소 | 후속 |
| `docs/poc/`·`docs/policy/` INDEX·자동 분류 | 1.3.8 은 prd·reports·ideation 만 | 후속 |
| `docs` 스킬의 자동 리네임·링크 재작성 도구 | fix-forward 로 충분, 도구는 과대 | 필요 시 후속 |
| founder 제품 PRD 8-section 재작성 | 역할 분리만, 내용 불변(§6 비범위와 연속) | — |

## 4. 손댈 파일

| 파일 | 변경 |
|---|---|
| `skills/docs/SKILL.md` | **신규** — `docs` 스킬 본체 |
| `skills/prd/SKILL.md` | §3.7 작성 5규칙(R1–R5) + HARD GATE + description (✅ 반영됨) |
| `skills/workflow-manager/assets/workflows/requirements-analysis/workflow.yaml` | §리뷰 게이트에 오버스펙 가드(R5) (✅ 반영됨) |
| `agents/main/product-manager/SKILL.md` | `skills_used` 에 `docs` 추가 |
| `scripts/check-docs-freshness.ts` | 4→6종 + PRD 존재 + docs/ 누출 불변식 + 승격 경로 |
| `docs/architecture.md` | `docs/prd/architecture.md` 에서 이동 + §1.3.8 |
| `docs/roadmap.md` | `docs/prd/product-roadmap.md` 에서 이동 + 릴리즈 row |
| `docs/reports/` | 신규 디렉토리 + 산재 리포트 이동 + `INDEX.md` |
| `docs/prd/INDEX.md`, `docs/ideation/INDEX.md` | 신규 인덱스 |
| `README.md` / `README.kr.md` | 승격 경로 참조 수정(architecture/roadmap) + 버전 |
| `manual/master-guide_{ko,en}.html` | 버전 헤더 + notes |
| `CHANGELOG.md` | `[1.3.8]` 항목 |
| `package.json` | `version` → `1.3.8`(CHANGELOG 동반) |
| `src/migrations/scripts/1.3.7-to-1.3.8.ts` | 연속성(경로 이동·reports 시드) |

## 5. 마이그레이션 영향

- `docs/prd/{architecture,product-roadmap}.md` 승격 → 참조 6곳 수정(README×2·게이트·
  PRD×2·ideation×1). 게이트는 backward-compat 로 구·신 경로 둘 다 허용 후 신 경로로.
- founder org 의 `<org>/docs/` 에 `reports/` 시드 + INDEX — `1.3.7-to-1.3.8.ts` 가
  dry-run/apply 로 처리(기존 마이그레이션 패턴).
- 발행분 PRD 명명은 immutable — 리네임 없음(fix-forward).

## 6. 설계 결정 (approaches 비교)

> PM 컨벤션 `minimum_approaches: 2` — 핵심 결정마다 ≥2 안 비교 + 권고 + 반증조건.
> 채택안은 §3 스코프가 구현하는 것. 사후 명명(post-labeling): 안을 먼저 처방하지
> 않고 후보를 나열한 뒤 명명.

**D1. 문서 관리 권위를 어디에 두나** — 채택: A
- A) 신규 `docs` 스킬(단일 권위 매니저) · B) 기존 `prd` 스킬 확장 · C) `docs/policy/`
  정책 문서 + 게이트 스크립트만(스킬 無).
- 권고 A. rationale: `prd`=per-PRD writer 와 큐레이션=cross-doc 권위는 관심사가
  다름(SRP) — 한 스킬에 합치면 두 양식(내부 dev/founder 제품) 혼동이 더 악화.
  C 는 "어디 배치·언제 최신화" 의 대화형 판단을 담을 주체가 없어 게이트가 *사후 거부*만
  하고 *사전 안내*를 못 함. 반증조건: 큐레이션이 거의 결정적(분기 없음)으로 밝혀지면
  대화형 스킬은 과설계 → B/C 로 회귀.

**D2. architecture/roadmap 의 위치** — 채택: A
- A) `docs/` 직속 living 문서로 승격 · B) `docs/prd/` 유지(현행).
- 권고 A. rationale: `docs/prd/` 의 의미축은 "버전 PRD = ship 후 frozen" 인데
  architecture/roadmap 은 계속 갱신되는 living 문서 → 생애주기 충돌·discovery 오염.
  반증조건: 참조 6곳 수정·INDEX 분리 비용이 분류 명확성 이득을 초과하면 B 유지.

**D3. 발행분 비표준 명명 처리** — 채택: A
- A) fix-forward(신규만 규칙 준수, 과거 유지) · B) 일괄 리네임.
- 권고 A. rationale: 발행분 파일명은 git 히스토리·외부 링크에서 immutable 취급 —
  리네임은 참조 깨짐 위험 대비 이득 미미. 반증조건: 자동 INDEX 생성이 명명 불일치로
  실패하면 깨지는 파일만 부분 리네임.

**D4. report 의 거처** — 채택: A
- A) `docs/reports/` 신설·집결 · B) `ideation/`·`trend-record/` 산재 유지.
- 권고 A. rationale: PRD 의 `evidence_ref` 인용 대상이 한 곳으로 모여 근거 추적이
  결정적. 반증조건: 리포트 수가 극소수면 신설 폴더가 빈 디렉토리 오버헤드.

## 7. 가정 분류 (V/U/V/F) + Open Questions

V/U/V/F = Valid(검증됨) · Unknown(미검증) · Vital(틀리면 설계 붕괴) · Falsifiable(반증 가능).

| 가정 | 분류 | 비고 |
|---|---|---|
| `package.json.files` 가 외부/내부 경계를 신뢰성 있게 강제 | **Valid·Vital** | publish 동작이 이미 의존(load-bearing) |
| founder org 들도 동일 외부/내부 축이 맞다 | **Unknown·Vital** | 솔로스쿼드 자기 docs 로만 검증, org 다양성 미확인 → OQ#2 |
| PRD ↔ 배포버전 1:1 이 항상 성립(한 버전 = 한 PRD) | **Unknown·Falsifiable** | 핫픽스·복수 PRD 동시 ship 시 깨짐 → OQ#1 |
| 큐레이션이 대화형 판단을 요한다(스크립트만으론 부족) | **Unknown** | D1 권고의 전제 — 반증 시 C 채택 |

Open Questions (→ `<org>/memory/open-questions/` append, blocking 표시):
1. **[blocking §3.4]** PRD↔버전 1:1 이 핫픽스(예: 1.3.8.1)·복수 기능 동시 ship 에서
   깨질 때의 규칙? (1 버전 = 1 PRD 의 예외 처리)
2. **[blocking §5]** founder org 의 docs 축이 솔로스쿼드와 다를 수 있나 — 마이그레이션이
   `reports/`·INDEX 를 강제 시드해도 되나, 옵트인이어야 하나?
3. roadmap 외부 공개 옵트인의 트리거는 누가·언제 결정하나? (§3.2 각주)
4. `docs/poc/`·`docs/policy/` 도 INDEX 대상인가? (§3.6 은 prd·reports·ideation 만 명시)

## 8. 비범위

- `docs/trend-record/`(요청 제외) · `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING`/`LICENSE`
  (human-edited 메타, 큐레이션 밖) · 워크플로 인스턴스 산출(`workflows/<id>/`).
- 발행분 비표준 명명 과거 리네임 · roadmap 의 강제 외부 공개.
- `prd` 스킬의 8-section/2양식 재작성(역할 분리만, 내용 불변).

## 9. 검증 (수용 기준)

**Success threshold (정량·게이트):**
- [ ] `npm run docs-check` 6종 + PRD 존재 + `docs/` 누출 불변식 = **green** (CI publish
      차단율 100% on 누락) — 측정 창: `prepublishOnly` 매 배포.
- [ ] 재정비 후 `docs/prd/` 내 비-PRD 파일 **0개**, `docs/reports/` 에 리포트 집결,
      prd·reports·ideation 각 `INDEX.md` 존재.
- [ ] PRD↔버전 1:1 + 명명 규칙(`<version>_<name>_<date>`) 강제 — 신규 PRD 가 규칙
      위반 시 `docs` 스킬이 생성 거부.

**품질 게이트:**
- [ ] `docs` 스킬이 `solosquad skill validate` + `primitive-review` 통과
      (description 공식·정량 한도·non-goal).
- [ ] 외부/내부 표(§3.2)가 `package.json.files` 와 1:1 일치.
- [ ] PM `skills_used` 라우팅으로 `docs` 해소(라우팅 테스트).

**Leading / Lagging:**
- Leading(구현 중): 마이그레이션 dry-run 이 참조 6곳을 전부 신 경로로 매핑.
- Lagging(배포 후): 분류 미준수(비-PRD 재유입)·게이트 우회 배포 발생 = §1.2 반증 신호.

**Open questions gate:** §7 OQ#1·#2(blocking)는 §3.4·§5 구현 착수 전 resolved 필요.
