# v1.3.1 — Legacy asset cleanup (on v1.3.0)

> **범위 안내:** 본 PRD 의 주제는 **구 `assets/` 정리** — v1.1 리오그가 절반만 끝내고 남긴
> 레거시 자산을 비우는 작업(§8·§9)이 중심이다. v1.3.0 출시 직후의 **post-release CI/deps
> 하드닝(§1~6)** 은 이 라운드를 촉발한 작은 선행 스코프라 같은 문서에 함께 박제한다.
> - **§1~6** — post-release CI/deps 하드닝 (촉발 스코프, 작음): audit 게이트·Node baseline·node-cron.
> - **§7** — skill-manager 기획 (`skill-author` 확장, 코드 0줄).
> - **§8** — stale `assets/agents/` 제거 (v1.1 이전 잔재 청소).
> - **§9** — 구 `assets/`(routines·knowledge·core·orchestrator·templates) → 최상위/번들 정리.

> **상태:** 정리/박제 (2026-06-16). v1.3.0 을 npm 에 publish + PR #8 머지하는
> 과정에서 **사전 존재하던** CI·의존성 문제가 머지 게이트에 드러나 동일 day 수정.
> 실제 작업은 main 의 commit chain `9efd3b9 → 417537f → 28f996c` (PR #8 에 포함되어
> 머지됨). 본 문서는 *역사적 박제* — v1.3.0 기능과 무관한 **빌드 파이프라인/의존성
> baseline 정합** 작업.
>
> **npm publish 여부:** 선택. 변경 3건 중 tarball 에 실제로 들어가는 것은
> `package.json.engines` 힌트(`>=18`→`>=20`)뿐이고(lockfile·CI 설정은 미배포),
> 그마저 better-sqlite3 가 이미 install 시 강제하던 사실의 명문화라 **별도 1.3.1
> publish 는 불요** — 다음 minor publish 에 자연 흡수해도 무방. 게시된 `solosquad@1.3.0`
> tarball 은 영향 없음(릴리스 커밋 `1880657` 기준, lockfile/CI/engines 동작 무변).

**로드맵 위치:** v1.3.0 (메신저 UX 대개편 — 게이트+인터랙션+P0/P1) → **v1.3.1 (본
post-release CI/deps 하드닝)** → v1.4.0 (세션 오케스트레이션).

---

## 1. 발견 경위

`solosquad@1.3.0` publish 후 PR #8(`feat/v1.3.0-messenger-ux-overhaul` → `main`)
머지를 시도하자 CI 가 전 플랫폼 red. v1.3.0 코드/의존성 변경이 유발한 게 아니라
**main 도 이미 같은 게이트로 red** 였던 사전존재 조건이, "머지 전 green" 기준 때문에
비로소 정면으로 드러난 케이스.

핵심 진단 함정 2개:
- **fail-fast 가 진짜 원인을 가렸다.** matrix `fail-fast`(기본 true) 때문에 Node 18
  잡이 `npm ci` 에서 죽으면 나머지 Node 20/22 잡이 *컴파일 도중 cancel* → "전부 fail"
  로 보여 어느 조합이 실제 통과하는지 알 수 없었음.
- **audit fail-fast 가 빌드 실패를 가렸다.** 첫 런에서 `npm audit` 게이트가 한 잡에서
  먼저 터지자 나머지가 cancel → Node 18/20 의 native-build 실패가 첫 런엔 안 보였음.

## 2. 근본 원인 (서로 다른 3건)

| # | 증상 | 근본 원인 |
|---|---|---|
| A | CI `npm audit --audit-level=high` 게이트 red (전 플랫폼) | 전이 의존성의 신규 고위험 권고 3건 — `esbuild`(Deno 바이너리 무결성 RCE / Windows dev-server 파일읽기), `form-data`(CRLF injection), `ws`(tiny-fragment DoS). v1.3.0 은 **직접 의존성을 추가하지 않음** → 권고는 시간이 지나며 publish 된 것이고 게이트가 뒤늦게 trip. |
| B | `npm ci` 가 Node 18 (ubuntu/windows) 에서 `better-sqlite3` 빌드 실패 | `better-sqlite3` 12.x 의 `engines` = `20.x \|\| 22.x \|\| …` — **Node 18 미지원**. Node 18 prebuilt 부재 → 소스 컴파일 폴백 → 툴체인 없는 러너(특히 Windows = VS 부재)에서 실패. 그런데 `package.json.engines` 는 `>=18` 로 **거짓 주장** 중이었음. |
| C | `npm ci` 가 Windows + Node 20 에서만 추가 실패 | `better-sqlite3` 12.x 가 win32 + Node 20(ABI 115) prebuilt 를 제공하지 않음 → 소스 빌드 → Windows 러너 VS 부재로 실패. (Node 22 win prebuilt 는 존재 → windows-22 통과. ubuntu-20 은 gcc 로 소스 컴파일 성공.) |

## 3. 영향

- v1.3.0 자체 기능/배포물은 정상(audit/CI 는 게시 tarball 에 미포함). 영향은 **레포의
  머지 게이트 + 향후 개발자 install 경험**에 한정.
- B 의 결과로 `engines: ">=18"` 은 사실과 불일치 — Node 18 사용자는 install 단계에서
  better-sqlite3 가 어차피 거부(또는 소스빌드 실패).

## 4. 변경 내역

### A — 고위험 권고 제거 (`9efd3b9`)
- `npm audit fix` (non-breaking, semver 범위 내) → `esbuild`(→0.28.1)·`form-data`·`ws`
  high 3건 해소 + `qs`·`js-yaml` moderate 동반 정리. **`package.json` 직접 의존성
  무변경, `package-lock.json` 만 변경.** 적용 후 `npm audit --audit-level=high` exit 0.
- 잔존: `uuid`(via `node-cron`) moderate 2건 — `--force`(node-cron major) 필요 +
  high 게이트 비대상이라 **후속**으로 분리.

### B — Node baseline 정합 (`417537f`)
- `package.json` `engines.node` **`>=18` → `>=20`** (better-sqlite3 현실 반영).
- CI matrix node **`[18,20,22]` → `[20,22]`**.
- `strategy.fail-fast: false` — 조합별 실제 결과를 노출(취소가 진짜 실패를 가리지 않게).
- CHANGELOG `[1.3.0]` 에 인프라 변경 박제(engines·audit).

### C — Windows+Node20 조합 제외 (`28f996c`)
- matrix `exclude: { os: windows-latest, node: 20 }` — better-sqlite3 win32 Node20
  prebuilt 부재 + 러너 VS 부재의 결정적 조합만 제거. **Windows 는 Node 22 로 커버 유지.**

## 5. 결과 / 검증

- 최종 CI: **5/5 green** — ubuntu(20·22) · macos(20·22) · windows(22).
- 로컬 `npx tsc --noEmit` · `npm run build` · `npm test` (790 pass) green 유지.
- PR #8 `--merge` 로 main 머지 (2026-06-16). 태그 `v1.3.0` 은 release 커밋
  `1880657` 을 그대로 가리킴(push 된 v\* 태그 강제 이동 금지 — 본 하드닝은 tarball
  동작 무변이라 retag 불요).

## 6. 비범위 / 후속

- ~~`uuid`(via `node-cron`) moderate 권고~~ → **해결 (2026-06-17).** `node-cron`
  `^3.0.3 → ^4.2.1` 업그레이드. v4 는 TS 재작성으로 `uuid` 의존성을 통째로 제거 →
  moderate 2건([GHSA-w5hq-g745-h8pq](https://github.com/advisories/GHSA-w5hq-g745-h8pq))
  소멸. `--force` 가 밀던 major(node-cron 4) 를 정식 채택했으나 **코드 변경 0줄** —
  유일 사용처 `src/scheduler/index.ts` 의 `cron.schedule(expr, fn, { timezone })` 가
  v4 에서 그대로 동작. `@types/node-cron`(devDep) 은 v4 내장 타입과 중복이라 제거.
  검증: `npm audit --audit-level=moderate` 0건 · `tsc --noEmit` · `npm run build` ·
  `npm test`(790 pass) green.
- **(추적 유지)** Windows + Node 20 네이티브 커버리지 복구(예: 러너에 build-tools
  설치 또는 better-sqlite3 prebuilt 갱신 추적) — 현재는 Node 22 로 Windows 커버 중이라
  리스크 낮음. CI 복구는 push→런 반복 검증이 필요하고 결과 불확실 → 추적 항목으로 보류.
- ~~moderate 권고를 게이트에 포함할지(`--audit-level=moderate`) 정책 재검토.~~ →
  **해결 (2026-06-17).** 하드 차단 대신 **비차단 가시화** 채택 — high 게이트는 그대로
  두고(`--audit-level=high`), 그 뒤에 `npm audit --audit-level=moderate || true` step 을
  추가해 moderate 권고를 로그에 노출만 함. "triage 전 배포를 막지 않는다"는 high 게이트
  정책을 유지하면서, 새 moderate(예: uuid/node-cron)가 조기에 보이게 함. (`ci.yml`)

---

## 7. skill-manager — `skill-author` 확장 (신규 후속, 기획)

> **위치 주의:** 본 §7 은 v1.3.1 의 원래 범위(CI/deps 하드닝)와 무관한 **신규 기능 기획**이다.
> 2026-06-17 SKILL.md 작성법 전수 조사([`docs/ideation/260617-skill-md-authoring-best-practices.md`])
> 에서 도출된 후속을 박제한다. 코드 0줄(기획만) — 실제 구현은 별도 브랜치/버전에서 진행해도
> 무방하며, 규모가 커지면 독립 PRD(예: v1.4.x)로 승격한다.

### 7.1 배경 / 문제

현재 `src/bot/skill-author.ts` 는 SKILL.md 를 **1-shot 생성**만 한다. 위 ideation 조사 결론:

- **검증 갭:** 우리 `validateSkill`(`skill-parser.ts`)은 공식/표준 규칙 다수를 미검사 —
  `name` ≤64자·`^[a-z0-9]+(-[a-z0-9]+)*$`·예약어(`anthropic`/`claude`)·**부모 디렉터리명 일치**,
  `description` ≤1024자·**3인칭(1인칭 린트)**·vague 휴리스틱·"Use when" 트리거절 부재.
- **개선 루프 부재:** 생성 후 품질을 *측정·개선*하는 경로가 없음. 업계는 이미 **SkillOpt**
  (MS, [arXiv](https://arxiv.org/pdf/2605.23904))식 rollout→reflect→bounded-edit→held-out
  검증 게이팅으로 SKILL.md 를 *학습*시킨다(스텝당 4–8 편집, 효과적 스킬 중앙값 ~920토큰).
- 즉 `skill-author` 라는 이름은 "생성"만 함의해 역할이 좁다.

### 7.2 변경: `skill-author` → `skill-manager`

`src/bot/skill-author.ts` → `src/bot/skill-manager.ts` 로 개명하고, 단일 "저자"에서 SKILL 의
**전 생애주기 관리자**로 확장한다. 4개 역량:

| 역량 | 동사 | 설명 | 토대(기존) |
|---|---|---|---|
| **생성(create)** | author | 표준 description 공식("`&lt;무엇을&gt;` — `&lt;구조&gt;`. `&lt;출처&gt;` 기반. 사용 시점: A,B,C. 제외: X")으로 SKILL.md 초안 생성. 3인칭·트리거 첫 문장·약간 pushy. | `skill-author.ts` |
| **검토(review)** | review | 생성/수정된 SKILL 을 ideation Part I 체크리스트로 LLM 검토 — description 품질, 본문 <500줄, 참조 1단계, non-goal 유무, 절차적 규율 중심성. | `skill-pr-review/`(PR 리뷰 기존) |
| **검사(validate)** | validate | §7.1 갭 포함한 정적 검증 강화(`validateSkill` 확장). 공식 정량/문체 + SoloSquad 확장(`schema_version`·slash 예약어·freq cap·`dev_permissions`). | `skill-parser.ts validateSkill` |
| **개선(refine)** | refine | SkillOpt 식 최적화 루프: corpus rollout → 성공/실패 반성 → **bounded edit(4–8/스텝)** → held-out 검증 시에만 채택 + 거부 buffer. protected-section 으로 내구 지침 보호. | `validator-corpus.ts` + `test:corpus`(eval 골격) |

### 7.3 구현 노트

- **개명 영향 범위:** `skill-author.ts` import 처 + `validate-skills`/author 호출부 grep 후 일괄.
  공개 CLI 표면(`solosquad agent …`)이 `author` 서브커맨드를 노출하면 하위호환 alias 유지.
- **검사(validate) 강화는 단독 선출고 가능**(P0, 가장 싸고 corpus 호환 ↑). 생성·검토·개선은 P1.
- **refine 는 우리 강점 재활용:** `skill-author`(=공식 "Claude A") + `validator-corpus`/`test:corpus`
  (=eval) 골격을 이미 보유 → SkillOpt 루프를 *얹는* 형태라 신규 인프라 최소.
- **검증 게이트 연동:** `npm run validate-skills`(현 `agent validate --all && test:corpus`)에
  강화된 validate + (선택) `skills-ref validate` 를 합류.

### 7.4 범위 / 비범위

- **In:** 개명 + 4역량(create/review/validate/refine)의 인터페이스 설계, validate 강화(P0).
- **Out(후속):** 마켓플레이스 배포 경로(`plugin.json`/kebab 정렬), 어휘 정렬(`dev_permissions`↔MCP
  annotations) — ideation Part H4·H5. stateful 스킬(v0.6 trajectory)과의 protected-section 통합.

---

## 8. stale `assets/agents/` 제거 (신규 후속, 정리)

> **위치 주의:** 본 §8 도 v1.3.1 의 원래 범위(CI/deps 하드닝)와 무관한 **레포 위생 정리**다.
> §7 작업 중 발견된 v1.1 리오그 잔재를 같은 day(2026-06-17) 정리·박제한다.

### 8.1 배경 / 문제

v1.1 리오그에서 캐노니컬 에이전트 로스터를 top-level `agents/`(main/ + specialists/, flat)
로 옮기고 팀 구성은 `teams/{team}/composition.yaml` 데이터로 분리했으나, **구 taxonomy
(팀 중첩) `assets/agents/`(25개, engineering/experience/growth/strategy)가 삭제되지 않고
잔존**했다. `src/cli/init.ts` 가 둘 다 `.solosquad/agents/` 로 복사 → 새 워크스페이스에
**서로 갈라진 로스터 2벌**이 섞이는 오염.

### 8.2 변경 내역

- **stale 로스터 삭제 (`9828a14`)** — `assets/agents/` 전체 제거(SKILL.md·KNOWLEDGE.md
  ~9,865줄). `init.ts` `assetDirs` 에서 `"agents"` 드롭 → v1.1 top-level 로스터만 복사.
- **죽은 collab_pattern 정리 (`9828a14`)** — `test/collab-pattern-frontmatter.test.ts` +
  `scripts/inject-collab-pattern.ts` 삭제. 삭제된 `assets/agents/` 셋에만 `collab_pattern`
  을 단언하던 false confidence(라이브 로스터는 미보유) + 런타임 미사용(마이그레이션 전용)
  → v0.6 §2.4 invariant 도 함께 은퇴.
- **maintainer 스크립트 경로 재지정 (`d425349`)** — `assets/agents/` 를 하드코딩하던
  frontmatter 스크립트(`backfill-bundled-frontmatter.ts`·`inject-dev-capability.ts`·
  `inject-skill-schema-version.ts`)를 캐노니컬 top-level `agents/`(`BUNDLE_AGENTS_DIR`)로
  재지정. stale doc-comment(`agents-builder.ts`·`frontmatter-diff.ts`)도 정정.
- **AGENTS.md** — v1.1 flat 에이전트 레이아웃 + `teams/` 를 캐노니컬로 문서화.

### 8.3 결과 / 검증

- `tsc --noEmit` · `npm test`(787 pass) · `agent validate --all`(25, 0 failed) green.
- 스크립트는 maintainer 일회성(SKILL.md mutate)이라 미실행 — 재지정은 컴파일로만 검증.

---

## 9. `assets/` → 최상위 잔여 이전 (✅ 완료)

> **위치 주의:** 본 §9 도 v1.3.1 의 원래 범위와 무관하다. §8 의 `assets/agents/` 제거로
> 드러난 "v1.1 이전이 절반만 끝났다"는 사실을 추적하던 항목으로, **2026-06-17 전부 해소**했다.
> routines·knowledge·core·orchestrator·templates 5종을 모두 정리해 `assets/` 에는 이전
> 대상이 아닌 `docker/`(+`.env.example`) 만 남았고, **v1.1 §1.2 "assets/ → 분산 완료"
> 체크박스가 닫혔다.**

### 9.1 배경

v1.1([`v1.1-multi-agent-team-architecture.md`] §1.1 항목 5·6)은 `assets/` 단일 폴더의
자산을 최상위로 분산하도록 설계했고, 마이그레이션은 **점진 이전**(`1.0.2-to-1.1.0.ts:39`
주석: *"assets/ directory deletion — bundle stays during transition"*)을 택했다. 에이전트
계층(agents/skills/teams/user)은 v1.1.0~v1.3.1 에 걸쳐 완료(§8 이 마지막 잔재 청소)됐으나,
**나머지 5종은 미완**이다. v1.1 §1.2 성공기준의 `[ ] assets/ → 5개 루트 폴더 + skill assets
분산 완료` 체크박스가 유일한 추적 지점이라 본 §9 로 현황을 박제한다.

### 9.2 잔여 현황

| 자산 | v1.1 목표 | 현재 | 리졸버 |
|---|---|---|---|
| agents / skills / teams / user | flat 최상위 | ✅ 완료(§8) | `getBundleRoot()` 계열 |
| docker | `assets/docker/` 단일 홈 | ✅ 완료(v1.2.10 Part D) — **이전 대상 아님** | `init.ts` 직접 |
| **routines → schedules** | `schedules/` | ✅ 해소(§9.4) — 스케줄러를 `getSchedulesDir()` 로 배선, `assets/routines/` 삭제 | `paths.ts getSchedulesDir` |
| **knowledge** | `knowledge/` | ✅ 해소(§9.4) — `getKnowledgeDir()` fallback → bundle, `assets/knowledge/` 삭제 | `paths.ts getKnowledgeDir` |
| **core** | org 레이어로 유지 | ✅ 해소(§9.4) — vestigial `assets/core/` + 죽은 `getCoreDir()` 삭제 | (제거됨) |
| **orchestrator** | 5종 분산 대상 | ✅ 해소(§9.4) — vestigial `assets/orchestrator/` 삭제, JSDoc 포인터 repoint | (제거됨) |
| **templates 폐지** | 각 skill `assets/` 분산 + bootstrap 만 `src/cli/init/templates/` | ✅ 완료(§9.5) — dead 15개 삭제 + live 7개 코드 상수로 인라인, `assets/templates/` 제거 | (제거됨) |

### 9.3 작업 노트 / 비범위

- **이중 상태(knowledge·routines) 우선 해소가 가장 싸다(P0).** 리졸버(`getKnowledgeDir`·
  `getRoutinesDir`)를 최상위(`getBundleRoot()/knowledge`·`/schedules`)로 재지정 + 옛
  `assets/` 사본 제거. 단 `getRoutinesDir` 를 참조하는 마이그레이션 스크립트(0.5.0/0.8.2)와
  스케줄러 경로 회귀를 함께 검증해야 함.
- **templates 분산은 가장 무겁다(P1).** 22개를 사용처 skill 의 `assets/` 로 흩고 bootstrap
  3종(AGENTS.md·agent-profile.yaml·hooks.json)만 init 전용 경로로 이관 — `goal.ts` +
  과거 migration 스크립트 다수가 `getAssetsDir()/templates` 를 직접 참조해 영향 범위 큼.
- **Out:** 남은 §9.5 항목(core·templates·orchestrator) 이전 시 번들 화이트리스트
  (`package.json.files`, v1.2.3 교훈) 회귀 + `npm pack --dry-run` seed-경로 grep 검증을
  동반해야 함. 규모상 v1.4.x/별도 PRD 승격 권장.

### 9.4 해소 기록 — knowledge · routines · core · orchestrator · templates(dead) (2026-06-17)

이중 상태를 조사하니 단순 "사본 2벌"이 아니라 **새 폴더를 만들고 배선을 안 한** 상태였다:

- **routines:** 스케줄러(`scheduler/routines.ts`)는 여전히 `getRoutinesDir()`(→`assets/routines/`
  4개)를 읽고 있었고, v1.1 이 신설한 `getSchedulesDir()` 는 **호출처 0개(죽은 함수)**, 최상위
  `schedules/`(7개)는 아무도 안 읽었다. 활성 4개(morning/evening/pm-compaction/system-housekeeping)
  는 양쪽 동일. 나머지 3개(`bot-health-check`·`leading-indicator`·`trace-rotate`)는 `ROUTINES[]`
  에 등록 안 된 **고아 파일**(v1.1 "신설" 계획만 있고 cron 미배선).
- **knowledge:** `assets/knowledge/README.md`(구 v0.6)와 최상위 `knowledge/README.md`(v1.1 신버전)
  공존. `getKnowledgeDir()` fallback 이 구버전(assets)을 가리켰다.

조치:

- `getKnowledgeDir()` 최종 fallback `getAssetsDir()/knowledge` → `getBundleRoot()/knowledge`.
- `loadRoutinePrompt()` 를 `getRoutinesDir()` → **`getSchedulesDir()`** 로 배선(죽은 함수를 정식
  경로로 승격). `getSchedulesDir()` 우선순위를 명시적 4단계로 재작성 — `.solosquad/schedules` →
  `.solosquad/routines`(v1.0.x 사용자 override 보존) → `schedules/` → `<bundle>/schedules`.
  기존 워크스페이스의 routines 커스터마이즈가 bundle 에 가려지는 회귀를 차단.
- `getRoutinesDir()` 는 back-compat 용으로 유지하되 fallback 을 `<bundle>/schedules` 로 재지정.
- `init.ts assetDirs` 에서 `"routines"` 드롭(이제 v1.1 bundle 블록이 `schedules/` 복사).
- `assets/knowledge/`·`assets/routines/` 삭제. 마이그레이션 스크립트(0.5.0/0.8.2)는 이미
  `fs.existsSync` 가드 + 참조 파일이 현 assets 에 부재(이미 no-op)라 회귀 없음.
- 테스트(`knowledge-loader.test.ts`) fallback 단언을 bundle 로 갱신.
- **고아 routine 3개는 보존**(harmless — `ROUTINES[]` 미참조라 로드 안 됨). 배선 여부는 별도
  결정 사항으로 남김.

**core (2026-06-17 추가 해소):** 조사 결과 `getCoreDir()` 역시 **호출처 0개(죽은 함수)**.
`assets/core/`(OWNER/PRINCIPLES/VOICE)는 v1.1 이후 아무도 안 읽는 vestigial — owner 페르소나는
`user/profile.md`·`user/voice.md` 로 승계됐고, 라이브 org 페르소나는 `<org>/core/{PRINCIPLES,VOICE}.md`
(init `scaffoldV06OrgLayer` + 0.5.0 마이그레이션이 inline stub 으로 생성, spawn-assembler [4] 레이어가
`orgDir/core/` 를 직접 읽음). 조치: `getCoreDir()` 삭제 + `init.ts assetDirs` 에서 `"core"` 드롭 +
`assets/core/` 삭제. lifecycle classify 의 "core"(CLASS_B_ORG_DIRS)·detect/doctor 의 v0.1.x 레거시
마커는 모두 **org/워크스페이스-루트 core** 라 무관. 재검증: tsc · build · test(787 pass) green.

**orchestrator (2026-06-17 추가 해소):** `assets/orchestrator/`(SKILL.md 219줄·goal-md-spec.md)는
v0.3 "You are the Chief" 정체성 문서로, v1.1 에서 **`agents/main/chief/SKILL.md`(160줄, v2
frontmatter)로 이전된 전신**. 후자가 `[kind:]` 마커·6+1 stage 머신·goal/workflow/schedule 라우팅을
모두 담은 캐노니컬이며 scaffold/마이그레이션이 `<org>/agents/main/chief/SKILL.md` 로 시드. 구
`assets/orchestrator/` 는 **런타임 fs read 0건**(init 이 `.solosquad/orchestrator/` 로 복사했으나
그것도 reader 없음). 실제로 `chief-runner.ts:309` 는 이미 같은 컨벤션을 chief SKILL.md 로 가리키고
있었고, `spawn-prompt-markers.ts`·`workflow-resolver.ts`·`goal-runner.ts` 의 포인터만 stale 했음.
조치: 3개 JSDoc 포인터를 `agents/main/chief/SKILL.md` 로 repoint + `init.ts assetDirs` 에서
`"orchestrator"` 드롭 + Step 8 layout preview 를 실제 v1.1 레이아웃으로 정정 + `assets/orchestrator/`
삭제. 재검증: tsc · build · test(787 pass) green.

**templates dead 제거 (2026-06-17):** 22개를 코드 fs-read / 번들·프롬프트 ref / 테스트 가드 기준으로
정밀 감사한 결과 **15개가 은퇴 시스템 잔재**로 확인돼 삭제(2차에 걸쳐):

*1차 (8개):*
- `session-{engineering,experience,growth,orchestrator,strategy}.md`(5) — pre-v1.1 per-team 세션
  템플릿. 런타임 read 0, 유일 ref 는 v1.1 PRD 문서 언급.
- `handoff-{dynamic,graph,hierarchical}.md`(3) — v0.6 §2.4 `collab_pattern` 변형 핸드오프.
  §8 에서 collab_pattern invariant 자체가 은퇴됐고, 런타임 read 0(코드의 hierarchical/graph/dynamic
  은 frontmatter enum 값일 뿐 템플릿 파일 미참조). 이를 지키던 `test/handoff-templates.test.ts`
  (v0.6 §2.4 S2 가드)도 §8 의 collab_pattern 테스트 삭제와 평행하게 제거.

*2차 (7개) — pre-v1.1 워크플로 스캐폴드:* `status.yaml`·`workflow.yaml`·`questions.md`·`handoff.md`·
`prd-{feature,experiment,pmf}.md`. 정밀 확인 결과 **템플릿 원본을 읽는 코드·테스트·SKILL.md 프롬프트가
모두 0**. 이름이 런타임 인스턴스(`_status.yaml`, `<org>/workflows/<wf>/workflow.yaml`)·skill 번들
정의와 중복돼 grep 매칭은 많지만, `assets/templates/` 원본 자체는 아무도 안 읽음 — v1.1 의 Chief/
specialist 가 이 형식들을 **각자 SKILL.md 프롬프트에 인라인으로 baked** 하면서 스캐폴드 파일이 잉여가 됨.
`prd-*.md` 는 `workflow-matcher` JSDoc 의 "키워드 출처" 언급뿐이라(키워드는 이미 코드에 하드코딩) 주석만
정정. handoff-variant 교훈을 반영해 (a)테스트 가드 (b)번들/docs ref (c)migration 실제 read 를 모두
사전 확인 후 삭제.

검증: `tsc --noEmit` · `npm run build` · `npm test`(782 pass) green.

### 9.5 templates live 7개 — 코드 인라인 완료 (2026-06-17)

dead 15개 제거 후 남은 **live 7개**는 "워크플로 템플릿"이 아니라 코드가 읽는 **코드/엔진/bootstrap
자산**이었다("template 개념 재정의" 논의). "공유 `templates/` 폴더"는 폐지하되 형식의 의의는 계승 —
관찰대로 SKILL.md 인라인(갈래 1) 후보는 이미 dead 로 빠져 없었고, 남은 7개는 전부 **읽는 코드에
문자열 상수로 인라인**(또는 immutable-path 정리)했다. **인라인은 내용이 `dist/*.js` 로 컴파일돼
번들에 항상 포함되므로, 파일 이동이 동반하는 번들 화이트리스트 회귀(v1.2.3 교훈)를 원천 차단**한다.

| 파일 | 처리 | read 사이트 |
|---|---|---|
| `migration-redestination-report.md` | 전체 내용을 `DEFAULT_REPORT_TEMPLATE` 상수로 인라인, fs-read 제거 | `0.5.0-to-0.6.0 writeRedestinationReport` |
| `goal.md` | `GOAL_TEMPLATE` 상수로 인라인, `getAssetsDir` import 제거 | `cli/goal.ts goalNewCommand` |
| `goal-from-skill.md` | `GOAL_FROM_SKILL_TEMPLATE` 상수로 인라인 | `bot/skill-author.ts renderGoalFromSkill` |
| `agent-profile.yaml` | migration 이 기존 `MINIMAL_AGENT_PROFILE_YAML` 상수만 사용(템플릿 복사 분기 제거) — fresh-install scaffold 와 일치 | `0.5.0-to-0.6.0 ensureOrgLayerStubs` |
| `AGENTS.md` | v0.4 섹션을 `AGENTS_V04_SECTION` 상수로 인라인, `extractV04Section`+`templatePath` 분기 제거 | `0.3.0-to-0.4.0` |
| `results.tsv` | 스키마는 `engine/tracker.ts` 주석에 존재 → 파일 삭제 + 두 immutable-path 기본값에서 stale `assets/templates/{results.tsv,goal.md,program.md}` 엔트리 정리 | `program-parser`·`agents-md-loader` (글롭) |
| `hooks.json` | 미배선 고아 예시(아무 코드도 read 안 함, manual ref 없음) → 삭제 | (없음) |

부수 정리: `init.ts` 의 `assetDirs` 복사 루프 제거(legacy `assets/<dir>` seed 전부 소멸) + Step 8
layout preview 정정. `workflow-matcher` JSDoc 의 prd-*.md 언급 정정.

검증: `tsc --noEmit` · `npm run build` · `npm test`(782 pass) green. `npm pack --dry-run` 으로
`assets/.env.example`·`assets/docker/` 정상 포함 확인(번들 회귀 없음). **`assets/` 에는 이제 `docker/`
+ `.env.example` 만 남아 v1.1 §1.2 체크박스 종료.**

> **결론:** "template 개념"은 사실상 v1.1 이 SKILL.md 인라인으로 이미 폐지했고, `assets/templates/`
> 에 남아있던 것은 분류가 어긋난 코드 자산들이었다. 이번에 전부 owning 코드로 인라인해
> `assets/templates/` 디렉토리 자체가 사라졌다.
