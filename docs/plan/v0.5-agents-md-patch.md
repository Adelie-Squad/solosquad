# v0.5 — `AGENTS.md` human-applied patch

Per `docs/plan/v0.5-workflow-maker.md` §10, the v0.5.0 release requires a
single human-applied edit to `AGENTS.md` line ~131. AI tools must not
modify `AGENTS.md` (immutable, human-edited only per `AGENTS.md` itself
under "Immutable paths — never modify"). This file holds the exact
replacement text the reviewer should paste before merging the v0.5 PR.

## What to replace

`AGENTS.md` currently contains (around L131, in the "Agent Routing"
section, after the v0.3 PM-mode narrative):

```
Legacy keyword routing (`src/bot/agent-router.ts`) is retained for the
scheduler and as future fallback — 60+ keywords → 25 agent mappings.
```

## Replacement text (single paragraph)

```
Keyword routing now lives in each SKILL.md's frontmatter (`triggers.keyword`),
discovered at boot by `buildRoutes()` in `src/bot/agent-router.ts` (v0.5). The
former hardcoded `AGENT_ROUTES` map is gone. The router scans the 3-tier
search path (org-local · user-global · workspace-bundled) and resolves
incoming messages via the 4-channel priority order: slash > explicit > keyword
> freq. Scheduler routines reference agents by name in their prompts rather
than going through router resolution.
```

## How to apply

1. Open `AGENTS.md` in your editor.
2. Find the "Legacy keyword routing…" paragraph (currently around L131,
   inside the `## Agent Routing` section).
3. Replace the entire paragraph with the text above. Preserve surrounding
   blank lines.
4. Commit as part of the v0.5.0 release PR with message:
   `docs(AGENTS): replace legacy AGENT_ROUTES paragraph with v0.5 frontmatter routing`.

No other changes to `AGENTS.md` are required for v0.5. The v0.6 §2.2
update — adding agent-profile.yaml and the 8-layer JIT spawn context —
ships in its own PR per the v0.6 plan.
